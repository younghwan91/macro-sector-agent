# 07 · 포트폴리오 구성과 매매계획 (L5)

## 1. 이 계층이 푸는 문제

L4 까지 오면 "무엇을" 은 정해진다. 남은 것은 **얼마나 · 언제 더 · 어디서 자르고 · 어디서 파는가**.
이 저장소에서 사용자의 최적화 배경이 실제로 값을 하는 유일한 계층이다.

핵심 제약 셋:
1. **MDD 30% 감내** — 이것은 목표가 아니라 **예산**이다. 다 쓰지 않으면 손해고, 넘기면 파산이다
2. **물타기 가능** — 진입 정밀도 요구가 낮은 대신, 사다리 전액이 예산 안에 들어가야 한다
3. **테마가 소수** — 유효 베팅 수가 1~2개로 붕괴하기 쉽다 (2026 원자재 케이스)

## 2. 정식화

### 2.1 결정 변수
- `w_i` — 종목 `i` 의 **목표 비중** (사다리 완납 시 최종 비중)
- `θ_i ∈ {1,2,3}` — 사다리 단계별 배분 (§3 에서 고정 비율, 최적화 변수 아님)
- 현금 `w_0 = 1 − Σw_i` (자유 변수, 하한 있음)

### 2.2 목적함수

```
max   Σ_i  c̃_{t(i)} · μ_i · w_i
 w
```

- `c_t` = 테마 `t` 의 `cycle_confidence` (`04-value-trap.md` §4) ∈ [0,1]
- `μ_i` = 기대수익 대용. **점추정을 신뢰하지 않는다** — 아래 강건화 참조
- `c_t` 는 §2.3 (2) 의 압축 변환 `c̃_t` 를 거쳐 들어간다 (기본 λ=0.3)
- `t(i)` = 종목 `i` 의 테마

**`μ_i` 를 어떻게 놓는가.** 이 저장소는 백테스트가 없으므로 `μ` 의 추정치가 없다.
세 가지 중 택일하되, **어느 것을 썼는지 리포트에 명시**한다:

| 방식 | 정의 | 언제 |
|---|---|---|
| (a) 균등 | `μ_i = 1` — 사실상 `c_t` 만으로 가중 | 기본값. 가장 정직 |
| (b) 밸류 회귀 | 자기이력 중앙값 밸류로 회귀 시 상승률 | 밸류 백분위가 신뢰 가능할 때 |
| (c) 사이클 진폭 | 직전 사이클 고점의 X% 회복 가정 | 케이스 스터디 근거가 있을 때 |

### 2.3 강건화 — 무엇을 철회하고 무엇을 두는가

`c_t` 는 캘리브레이션되지 않은 주관 확률이다 (`10-validation.md` §4 이 이를 측정한다).
이 불확실성을 목적함수 쪽에서 흡수하려던 이전 설계를 **철회한다.**

> **철회한 것 하나 — 박스 강건화는 선형 목적에서 의도한 것과 정반대로 작동했다.**
> 이전 판은 `U(c,ε) = { c̃ : |c̃_t − c_t| ≤ ε_t, ε_t = 0.15 }` 위에 분포적 강건
> 카운터파트를 세우고, 해가 `c_t − ε_t` 결정론 문제와 같다고 적었다. **여기까지는 맞다.**
> 틀린 것은 그 다음 문장 — "ε 를 크게 잡을수록 배분이 균등해진다" — 이다.
>
> 목적함수가 `w` 에 대해 선형이고 계수 `c_t·μ_i` 가 전부 양수이므로, 균일한 상수 ε 를
> 빼는 것은 계수의 **변동계수를 키운다.** `c = 0.85 / 0.55` 일 때 ε=0.15 면 0.70/0.40 (비 **1.75**),
> ε=0.40 이면 0.45/0.15 (비 **3.0**). ε 를 키울수록 저확신 테마가 눌리고 **배분이 더 집중된다.**
> ε ≥ min(c) 가 되면 저확신 테마의 계수가 0 이하가 되어 **아예 탈락**한다 — 최대 집중.
>
> 총투자액(gross)도 줄지 않는다. 계수가 전부 양수인 한 해는 여전히 C1(리스크) 또는
> C5(현금하한) 경계에 붙는다. **보수화 효과는 0 이고, 실제 효과는 집중화뿐이었다.**
>
> 근본 원인: `c` 는 목적함수 계수일 뿐 제약에 들어가지 않는다. 선형계획에서 목적함수
> 계수의 **균일 평행이동은 상대 순위를 바꾸지 않고 비율만 왜곡**한다. 규약대로 조용히
> 지우지 않고 무엇이 왜 틀렸는지 남긴다 (`portfolio-research` 의 "철회를 정정한다" 선례).

**대신 두는 것.**

**(1) 기본은 `c` 를 그대로 쓴다.** `c` 가 캘리브레이션되지 않았다는 사실은 목적함수가 아니라
**C6 최소 확신도 컷오프**(편입 자체를 막는다)와 **`10-validation.md` §4 의 사후 캘리브레이션**
(값 자체의 정확도를 잰다)이 담당한다. 목적함수를 만지는 것은 둘 중 어느 역할도 하지 못한다.

**(2) 확신도의 과대 추정이 걱정되면 평행이동이 아니라 압축을 쓴다.**

```
c̃_t = c̄ + (1 − λ)(c_t − c̄),   c̄ = 편입 후보 테마의 c 평균,   λ ∈ [0,1]

λ = 0  원래 확신도 그대로
λ = 1  완전 균등 (확신도 차이를 전부 잡음으로 간주 — 사실상 c 무시)
```

계수의 **평균은 보존하고 분산만 줄이므로**, 이것이 이전 판이 의도했던 "배분이 균등해진다" 를
실제로 구현하는 형태다. λ 의 해석도 명확하다 — **"확신도 차이를 몇 % 신뢰하는가."**
선형 목적이므로 변환 후에도 문제는 선형이 유지된다.

**기본값 λ = 0.3 (선언값).** 근거: 캘리브레이션 표본이 `10-validation.md` §4 의 기준선인
**N = 20 에 도달하기 전까지는 확신도 차이의 상당 부분이 잡음이라고 보는 것이 정직**하다.
곡선을 그릴 수 없는 동안 `c` 의 차이를 액면 그대로 신뢰할 근거가 없다. **탐색으로 얻은 값이
아니다** (`CLAUDE.md` §1). N ≥ 20 이후의 대체 경로는 §7.2 에 적는다.

**(3) 강건화가 정말 필요한 곳은 `c` 가 아니라 제약 쪽의 추정오차 — 공분산 `Σ` 와 시나리오 손실 `L_i` 다.**
이 포트폴리오를 실제로 죽이는 경로는 확신도가 0.1 틀리는 것이 아니라 **스트레스 국면에서
상관이 1 로 수렴하는 것**이고, 그것은 목적함수가 아니라 C1 이 막는다.
C1-(ii) 시나리오 제약이 이미 그 역할을 맡고 있으므로 **1차 방어선으로 승격**한다 (§2.4 C1).
(KL·Wasserstein 같은 정교한 집합은 `c` 의 분포를 모르는 현 단계에서 여전히 과잉이다.)

### 2.4 제약

**C1 · MDD 예산 (핵심)**

두 가지 방식을 **둘 다** 계산하고 **더 보수적인 쪽**을 구속으로 쓴다.

```
(i) 변동성 기반:   MDD_proxy = k · σ_p ≤ 0.30,   σ_p = √(wᵀ Σ w),  k = 2.2

(ii) 시나리오 기반: Σ_i w_i · L_i ≤ 0.30
     L_i = 테마 `t(i)` 의 "논지 실패 시나리오" 손실
         = max(과거 유사 국면 최대 낙폭, 케이스 스터디 사망 사례 낙폭 × 0.5)
```

`k = 2.2` 근거: 1년 보유·정규 가정에서 최대낙폭 기대치는 대략 `2σ` 근방이며,
사이클주의 두꺼운 왼쪽 꼬리를 감안해 상향했다. **탐색으로 얻은 값이 아니다** (`CLAUDE.md` §1).

(ii) 를 함께 쓰는 이유: 변동성 기반은 **상관이 1로 수렴하는 국면**을 과소평가한다.
2026 원자재 4종목의 실제 스트레스 상관은 0.8~0.9 였다.

> **C1-(ii) 가 이 설계의 1차 방어선이다.** C2 를 구속에서 뺐고(아래), `c` 에 대한
> 박스 강건화를 철회했으므로, 상관 집중 위험을 실제로 가격에 반영하는 물건은
> σ_p 와 시나리오 손실 두 가지뿐이다. 그중 스트레스 상관 1 수렴을 **직접** 모델링하는 것은
> (ii) 다. 그 결과 **`L_i` 추정의 중요도가 올라갔다.**
> `L_i` 는 케이스 스터디(`04-value-trap.md` §5)의 사망 사례 낙폭에 의존하므로,
> **그 6건 작성은 M6 의 선행 조건에 가깝다** — 케이스가 없으면 (ii) 는 숫자를 만들 수 없다.

**`L_i` 의 `× 0.5` 에는 근거가 없다 — 그렇다고 적는다.**

이 계수에 댈 수 있는 논리는 하나뿐이었다. "사망 사례의 낙폭을 그대로 쓰면 사이클 테마에
사망 시나리오를 부과하는 것이므로 절반으로 깎는다." **검토 결과 이 논리는 성립하지 않는다.**
(ii) 의 `L_i` 는 애초에 **"논지 실패 시나리오" 의 손실**로 정의되어 있다. 조건부가 이미
"논지가 틀렸을 때" 로 걸려 있으므로, 사망 사례를 부과하는 것은 과잉이 아니라 **바로 그 조건이
실현된 경우**다. 절반으로 깎는 것은 (ii) 가 막으려던 꼬리를 정확히 그만큼 과소평가한다.

그렇다고 여기서 `× 1.0` 으로 올리지 않는다. 그것도 근거 없는 값이고, `CLAUDE.md` §1 이
금지하는 것은 근거 없는 값이 아니라 **탐색으로 값을 정하는 것**이므로, 값을 바꾸는 것보다
**근거가 없다는 사실을 적는 것**이 규약이 허용하는 처리다. 다음과 같이 둔다.

| 항목 | 상태 |
|---|---|
| `× 0.5` | **근거 없음.** 선언값이며 도메인 근거 칸이 비어 있다 |
| 오차의 방향 | 알려져 있다 — 계수가 작을수록 `L_i` 가 작아져 **제약이 느슨해진다.** 안전한 쪽으로 틀린 값이 아니다 |
| 재검토 시점 | **M6, 케이스 스터디 6건이 실제로 작성될 때** (`04-value-trap.md` §5). 그전에는 (ii) 가 쓸 낙폭 숫자 자체가 없다 |

그때까지 매매계획서는 `L_i` 를 하나의 숫자로 적지 않는다 — **max() 의 두 항(과거 유사 국면
최대 낙폭 · 사망 사례 낙폭 × 0.5)과 어느 쪽이 구속했는지를 함께 표기**한다.
근거 없는 계수가 결과를 뒤집었는지 사용자가 직접 볼 수 있어야 한다.

**C2 · 유효 베팅 수 (Effective Number of Bets)** — 논점 ④ 의 해법. **구속 제약 아님 · 리포트 필수 지표**

포트 리스크를 잠재 팩터로 분해:

```
Σ = 구성종목 수익률의 공분산 (252일, Ledoit-Wolf 축소)
고유분해 Σ = V Λ Vᵀ,  팩터 노출 y = Vᵀ w,  리스크 기여 p_k = λ_k y_k² / (wᵀΣw)

ENB = exp( − Σ_k p_k log p_k )          (분산 엔트로피)
```

> **N종목 보유가 아니라 ENB 를 본다.** 2026 원자재 4종목의 ENB 는 1.4 정도였을 것이다.
> 4개를 들고 있었지만 베팅은 1.4개였다. 30% 수익의 이유이자, 뒤집혔으면 −50% 였을 이유다.
> **이것은 계속 재는 값이다.** 바뀐 것은 이 값을 최적화기에 구속으로 물리지 않는다는 점뿐이다.

**왜 구속 제약에서 뺐는가.** 세 가지 이유가 겹친다.

1. **이 저장소가 자기 원형 사례를 배제한다.** `00-overview.md` 는 "분산으로 알파를
   희석할 이유가 없다" 고 적었고, 원형 사례인 2026 원자재 4종목의 ENB 는 이 문서 스스로
   1.4 로 추정했다. ENB ≥ 3.0 을 기본 구속으로 걸면 **그 포트폴리오는 애초에 infeasible** 이다.
   설계가 자기가 설명하려던 사례를 못 만드는 것은 제약이 틀렸다는 신호다.
2. **3.0 에 도메인 근거가 없다.** 선언값인데 근거 칸이 비어 있었다. `CLAUDE.md` §1 은
   근거 없는 임계값을 허용하지 않는다 — 근거가 없으면 없다고 적거나, 구속에서 내려야 한다.
3. **같은 위험을 두 번 센다.** 상관 집중의 대가는 C1 이 이미 가격에 반영한다 —
   `σ_p = √(wᵀΣw)` 는 상관이 높으면 자동으로 커지고, C1-(ii) 는 스트레스 상관 1 수렴을
   직접 모델링한다. ENB 구속은 그 위에 **중복 제약**을 얹어 해를 두 번 깎는다.

**대신 이렇게 한다 — 눈금 없이, 그러나 항상 보이게.**

ENB 는 매매계획서에 **항상** 표기하되, **"몇 이하가 위험하다" 는 임계는 두지 않는다.**
함께 적는 것은 **상위 3개 고유값의 리스크 기여 비중 `p₁ · p₂ · p₃`** 다.
ENB 는 이 분포를 한 숫자로 압축한 값이므로 압축 전의 것을 같이 보여 주면 사용자가 집중의
형태를 직접 읽는다 — `p₁ = 0.58` 은 "한 팩터가 리스크의 58% 를 지고 있다" 로 그대로 읽히지만,
`ENB = 2.1` 은 눈금을 알아야 읽힌다.

**임계를 없애는 쪽을 고른 이유.** 선택지는 둘이었다.

| 선택지 | 내용 | 판단 |
|---|---|---|
| (a) 경고 임계 2.0 에 근거를 단다 | 댈 수 있는 근거는 하나뿐이다 — 2026 원자재 4종목의 ENB ≈ 1.4 가 걸리는 눈금. "원형 사례가 경고에 걸린다" 는 근거로 성립은 하지만 **표본 1개에서 임계를 정하는 것**이다 | 기각 |
| (b) 임계를 없앤다 | ENB 를 **눈금 없는 관측값**으로 두고, 경고 대신 `p₁·p₂·p₃` 를 항상 표기한다 | **채택** |

`ENB ≥ 3.0` 을 구속에서 내린 사유 2번이 "근거 없는 임계값" 이었다. 그 자리에 N = 1 에서 뽑은
임계를 놓으면 **같은 결함이 값만 바꿔 남는다.** "몇 이하가 위험한가" 를 선언하지 않는 것이
근거 없이 선언하는 것보다 정직하고, 표기하는 정보량도 (b) 쪽이 더 많다.

> **경고를 없앤 것이 "모르고 하는 집중" 을 허용하는 것은 아니다.**
> 원래 경고가 하던 일은 사용자가 집중 상태를 **알고서** 승인하게 만드는 것이었고,
> 그 일을 하는 것은 임계가 아니라 **표기**다. 임계는 오히려 그 위쪽이면 조용히 넘어가도 된다는
> 눈금을 준다 — `ENB = 2.1` 이 무해하다는 근거는 어디에도 없었는데, 이전 판의 계획서는
> "경고 임계 2.0 — 미발동" 이라고 적어 무해함을 시사했다.
> 집중의 대가는 C1 이 이미 청구하고 있다 (상관이 높으면 σ_p 가 커져 포지션이 자동으로 줄어든다).

**C3 · 집중도 상한**
```
단일 종목      w_i ≤ 0.15
단일 테마      Σ_{i∈t} w_i ≤ 0.35
단일 cycle_class Σ ≤ 0.55        # commodity_supply 에 전부 몰리는 것 방지
```

**C4 · 유동성**
```
w_i · Capital ≤ 0.10 · ADV20_i         # 20일 평균 거래대금의 10%
```

> ⚠ **자본을 주지 않으면 이 제약은 걸리지 않는다 — 그리고 자본에는 기본값이 없다.**
> `msa portfolio --capital` 의 기본값은 0 이고 그것은 `capital_usd=None` 으로 들어간다.
> `msa run monthly` 도 `--capital` 을 넘기지 않으면 마찬가지다. **식에 `Capital` 이 없으면
> 부등식을 세울 수 없으므로 C4 는 한 번도 걸리지 않는다** — "핵심 제약" 이라 부른 것이
> 실운영 기본값에서 무효라는 뜻이다.
>
> 여기서 **자본 기본값을 정하지 않는다.** 그 값은 사용자의 계좌 크기이고, 문서에 선언된 값이
> 아니며, 코드가 고르면 `CLAUDE.md` §1 이 금지하는 발명이다. 대신 **적용 여부를 보이게** 한다 —
> 계획서 본문 머리에 `C4 유동성: 미적용 — 자본(--capital) 미지정 → w·Capital ≤ 10%·ADV20 을
> 걸지 않았다` 가 찍히고, 경고란에도 같은 사실이 남으며, `diagnostics.json` 의 `c4_applied` 가
> `false` 다. 자본을 주면 같은 자리에 `적용 — 자본 $N` 과 ADV 결측으로 **제외된 종목 목록**이
> 찍힌다. C4 가 실제로 걸린 산출물을 원하면 `--capital` 을 주는 것이 유일한 경로다.

**C5 · 사다리 완납 가능성**
```
Σ_i w_i ≤ 1 − cash_floor,   cash_floor = 0.15
```
사다리 2·3단은 목표 비중 `w_i` 안에 포함되므로, **초기 진입 시점의 실투입은 `Σ 0.5·w_i` 다.**
현금이 남는 것이 정상 상태이며, 이는 "기회 손실" 이 아니라 **사다리의 원자재**다.

**C6 · 최소 확신도**
```
c_{t(i)} ≥ 0.50  인 테마만 편입 가능        # 04 §3 하드 게이트와 연동
```

### 2.5 풀이

선형 목적 + (C1-i 제외) 선형 제약 + C1-i 의 2차 원뿔 → **SOCP**.
C2 가 구속이 아니므로 **기본 경로는 SOCP 한 번**이다. 풀고, ENB 를 계산해 리포트에 싣는다.
infeasible 이면 완화 순서는 **C3 → C1** 이다 (`09-operations.md` §5).

**선택적 절차 — 클러스터 상한 (사용자가 명시적으로 요구했을 때만).**
사용자가 "이 클러스터에 상한을 걸어 달라" 고 요구하거나, 표기된 ENB · `p_k` 를 보고 재구성을 원하면,
ENB 를 직접 제약하는 대신(비볼록이다) 다음을 돈다:

1. SOCP 를 푼다
2. **상관 클러스터별 상한**을 추가하고 재해를 구한다
   (계층적 클러스터링으로 상관 0.7 이상 그룹을 묶고 그룹 합에 상한)
3. 2를 **사용자가 그 자리에서 정한 목표 ENB** 가 만족될 때까지 반복 (보통 1~2회)

> 이 근사가 정확한 ENB 최적화보다 나은 이유는 그대로다: 클러스터 상한은 **읽을 수 있다.**
> "금·은·PGM 클러스터에 35% 상한" 은 사용자가 검토하고 뒤집을 수 있는 결정이지만,
> 엔트로피 제약의 쌍대변수는 그렇지 않다. **이것이 이 절차가 존재하는 이유이며,
> 기본으로 돌지 않는 것과는 별개다** — 사용자가 분산을 원할 때 쓸 손잡이는 남겨 둔다.

> ⚠ **위 3단계 절차 중 실제로 구현된 것은 2 의 절반뿐이다** (2026-08-24 확인).
> 코드(`l5/optimize.py`)가 거는 것은 `--cluster-cap name=cap` 으로 **사용자가 이름을 대고 준**
> 상한이며, 클러스터 자체는 `state/themes.yaml` 의 정적 `correlation_cluster` 필드다.
> **없는 것 둘**: (i) "계층적 클러스터링으로 상관 0.7 이상 그룹을 묶는" 코드 — 종목/테마 상관에서
> 그룹을 만드는 함수가 없다. (ii) 3 의 "목표 ENB 만족까지 1~2회 반복" 루프 — 최적화기는 한 번
> 풀고 끝난다(완화 단계는 infeasible 처리이지 ENB 반복이 아니다). 만들지 않았다 —
> 임계도 클러스터링 파라미터도 문서에 선언된 값이 아니고, 여기서 정하는 것은 `CLAUDE.md` §1
> 위반이다. **없는 것을 없다고 적는 쪽**을 골랐다.

## 3. 물타기 사다리

**가장 중요한 규칙이 하나 있다.**

> ⚠ **물타기는 가격이 아니라 논지 상태에 조건부다.**
> 무효화 트리거가 하나라도 발동하면 **추가 매수 금지**, 청산 검토로 전환한다.
> 가격만 보고 물타는 것이 가치함정에서 자본이 사라지는 정확한 경로다.

기본 사다리 (`cycle_confidence` 에 따라 조정):

| 단계 | 목표비중 대비 | 발동 조건 (**둘 다** 필요) |
|---|---|---|
| 1단 진입 | 50% | 논지 확정 시점. M 축이 낮으면 25%+25% 로 분할 |
| 2단 추가 | 30% | 가격 −12~15% **AND** 무효화 0건 **AND** 트리거 1건 이상 충족 |
| 3단 추가 | 20% | 가격 −22~25% **AND** 무효화 0건 **AND** 트리거 진행 중 |

확신도 연동:
```
c ≥ 0.75 :  60 / 25 / 15    (확신 높으면 앞에 싣는다)
c ∈ [0.6, 0.75) : 50 / 30 / 20
c ∈ [0.5, 0.6) : 35 / 35 / 30    (확신 낮으면 뒤로 미룬다)
```

## 4. 스탑 — 2단 + 시간

가격 스탑 하나만 두면 사이클 논지 + 물타기와 **자기모순**이다 (물타는 지점에서 스탑이 걸린다).
세 종류를 분리한다.

| 종류 | 발동 | 조치 | 근거 |
|---|---|---|---|
| **Tier 1 · 논지 스탑** | `thesis.invalidations` 중 1건 관측 | **즉시 전량 청산. 가격 무관** | 논지가 죽으면 보유 이유가 없다 |
| **Tier 2 · 자본 스탑** | 사다리 평단 대비 −35%, 또는 포지션 손실이 총자본의 8% | 전량 청산 | 논지 판단이 틀렸을 경우의 최후 방어선 |
| **시간 스탑** | `horizon_months` 상한 경과 **AND** 충족 트리거 0건 | 전량 청산 | ↓ |

> **시간 스탑이 가장 자주 무시되고 가장 중요하다.**
> 가치함정은 급락으로 자본을 죽이지 않는다. **정체로 죽인다.**
> 18개월 동안 −10% 에 머문 포지션은 손실이 작아 보이지만, 그 기간의
> 기회비용과 심리적 앵커링이 다음 사이클을 놓치게 만든다.
> 트리거가 하나도 안 맞았다는 것은 **메커니즘이 틀렸다**는 뜻이다.

**Tier 2 의 −35% 근거 — 예산에서 역산한 값이다.**

단일 테마 최악 시나리오의 손실 기여는 테마 상한 × 포지션 손실률이다.

```
테마 상한 0.35 × Tier2 손실 0.35 = 총자본의 12.25%
12.25% / 30% 예산 = 40.8%
```

즉 **한 테마가 완전히 틀려서 Tier2 까지 맞아도 MDD 예산의 41% 만 쓴다.**
예산의 절반 이하로 유지되므로 나머지 테마가 동시에 흔들려도 30% 안에 머물 여지가 남는다.
−35% 는 이 관계를 만족하는 값으로 정한 것이지 **탐색해서 얻은 값이 아니다** (`CLAUDE.md` §1).

**평단은 사다리마다 다르다.** §3 의 확신도 연동 사다리를 2단 −13% · 3단 −23% 에
완납했다고 놓고 계산하면 (초기 진입가를 1.0 으로 정규화):

| 사다리 (c 구간) | 평단 계산 | 평단 | 초기가 대비 | Tier2 발동가 (평단 −35%) | 초기가 대비 |
|---|---|---|---|---|---|
| 60/25/15 (c ≥ 0.75) | 0.60(1.00)+0.25(0.87)+0.15(0.77) | 0.9330 | −6.7% | 0.6065 | **−39.4%** |
| 50/30/20 (c ∈ [0.6,0.75)) | 0.50(1.00)+0.30(0.87)+0.20(0.77) | 0.9150 | −8.5% | 0.5948 | **−40.5%** |
| 35/35/30 (c ∈ [0.5,0.6)) | 0.35(1.00)+0.35(0.87)+0.30(0.77) | 0.8855 | −11.5% | 0.5756 | **−42.4%** |

확신도가 낮을수록 뒤에 싣기 때문에 평단이 낮아지고, **초기가 기준 스탑은 더 멀어진다.**
의도한 방향이다 — 확신이 낮은 논지일수록 사다리를 다 써 볼 여지를 준다.
다만 손실 기여는 세 경우 모두 위 12.25% 계산으로 동일하다. Tier2 가 **평단 기준**이고
테마 상한이 동일하기 때문이다. 계획서에는 **초기가 대비 %** 도 함께 적는다 —
사용자가 실제로 보는 것은 진입가이지 평단이 아니다.

> 이전 판은 평단을 −13% 로 적고 초기가 대비 −44%, 손실 기여 약 15% 라고 적었다.
> −13% 는 **2단 진입가를 평단으로 착각**한 값이다 (2단 트리거가 −13% 다).
> 결론 — "단일 테마 최악 시나리오가 예산의 절반 이하" — 는 올바른 산술 위에서도
> 그대로 성립하지만(41%), 숫자는 틀려 있었으므로 정정한다.

## 5. 익절 (TP) 사다리

원자재·사이클은 **수익의 대부분이 마지막 구간**에 나온다. 전량 익절이 최악이다.

| 단계 | 물량 | 조건 |
|---|---|---|
| TP1 | 1/3 | 테마 밸류 백분위 P50 회복 **또는** +2R |
| TP2 | 1/3 | 테마 밸류 백분위 P75 **또는** 직전 사이클 고점의 50% 회복 |
| 러너 | 1/3 | 트레일링 — 주간 10주선 이탈 또는 고점 대비 −25% |

`R` = 초기 진입가 − Tier2 스탑가 (리스크 1단위).

**전환 규칙**: TP1 체결 후 Tier 2 스탑을 **본전(사다리 평단)으로 상향**한다.
이 시점부터 포지션은 리스크 프리이며, MDD 예산에서 해당 분이 해제되어
**새 테마 편입 여력이 생긴다.** 포트 회전의 자연스러운 엔진이다.

> ⚠ **본전 상향까지가 구현이고, "예산 해제 → 새 테마 여력" 은 미구현이다** (2026-08-24 확인).
> `msa check` 는 TP1 체결을 보면 `tier2_basis` 를 `breakeven` 으로 올린다 (`ops/check.py`).
> 그러나 **예산 해제는 최적화기가 해야 하는 일인데 최적화기는 기존 보유를 모른다** — `msa
> portfolio` 는 매번 빈 포트에서 새로 푼다 (§7.4 가 이미 인정한 것과 같은 결손). 그래서 오늘
> "리스크 프리가 된 포지션의 몫만큼 C1 예산이 풀린다" 는 일은 **일어나지 않는다.** 회전 엔진은
> 문장으로만 있다. 만드는 것은 §7.4 를 닫는 일이고 여기서 앞당기지 않는다.

## 6. 산출물 — 매매계획서

```
──────────────────────────────────────────────────────────────────────────────
포트폴리오 계획 · 2026-08-21 · 자본 대비
  MDD 예산 사용률: 25.2% / 30%      ENB: 1.9  (p₁ 0.74 · p₂ 0.24 · p₃ 0.02)
  MDD 방식: 변동성 k·σ_p = 25.2% (k=2.2) · 시나리오 Σw·L = —  → 둘 다 여유 (다른 상한이 구속)
  앵커 : 토크 = 라벨 없음 (L4 미지정)          현금 55%  (총투자 45%, 1단 실투입 23%)
  C4 유동성: 미적용 — 자본(--capital) 미지정 → w·Capital ≤ 10%·ADV20 을 걸지 않았다
  Tier-2 예산 역산: 테마 상한 35% × Tier2 35% = 총자본 12.25% = MDD 예산 30% 의 40.8% (docs/07 §4)
  μ 방식: (a) 균등                  확신도 압축 λ = 0.3
  솔버: CLARABEL (optimal) · 완화: 없음
  Σ: theme_ew_monthly · 룩백 60 · 상수상관 축소 δ=0.5 · 2021-08-31~2026-07-31
──────────────────────────────────────────────────────────────────────────────
uranium  (c=0.72 [사람], c̃=0.71, 축1 가능)                       테마 30.0%
  L = 계산 불가  (과거 유사 국면 0.58 / 사망 사례 —) → C1-(ii) 에서 빠짐 — 케이스 스터디:
      테마 uranium (클러스터 nuclear) 에 해당하는 행 없음
  CCJ    적격    목표 15.0%   사다리 7.5% / 4.5% / 3.0%  (50%/30%/20%)
         진입 $100.00   2단 $87.00(−13%)   3단 $77.00(−23%)   — 2·3단은 무효화 0건 AND …
         Tier1  쿼터 +20% 발표 [WNA]
         Tier2  $59.48 (평단 −35% = 초기가 −40.5%)   시간스탑  2028-02-21 (horizon 18M, …)
         TP1 $181.05 (+2R)  TP2 $— (P75·직전 고점 입력 없음)  러너 트레일 25% / 10주선
         트리거  현물 가격 상승 [UxC]
──────────────────────────────────────────────────────────────────────────────
경고
  · C4 유동성 미적용 — 자본(--capital) 미지정 → `w·Capital ≤ 10%·ADV20` 을 한 번도 걸지 않았다 …
  · 앵커:토크 비율 없음 — L4 가 바벨 라벨을 붙이지 않는다 (role 이 전부 `eligible`) …
  · 공분산: 종목 수익률 없음 → … 고유분산 0 인 것이 2/3종목 ['UEC', 'PWR'] …
  · 테마 EW 수익률에 결측일이 있다 — 수익률 0 으로 메우고 계산했다 (uranium 7일) …
  · cycle_class commodity_supply 합계 30.0% (상한 55%)
```

**Tier-2 줄은 적용된 규칙의 가격과 하락률을 짝으로 찍는다.** 자본 8% 규칙이 이기면
(완화로 비중이 0.2286 을 넘을 때) 이렇게 나온다 — 가격은 자본 규칙, % 도 자본 규칙이다:

```
         Tier2  $201.30 (자본 8% 규칙이 먼저 = 초기가 −32.9%; 평단 −35% 는 $178.43 =
                초기가 −40.5% 로 더 멀다)   시간스탑  2028-02-21 (horizon 18M, …)
```

**이전 판의 예시에 있던 `CCJ·UEC 상관 0.81 — 동일 클러스터로 처리됨` 경고는 미구현이다**
(2026-08-24 확인). **종목 쌍 상관을 계산해 경고로 내는 코드가 없다.** 계획서가 상관에 대해
말하는 것은 `Σ` 줄(출처·룩백·축소)과 ENB·`p₁·p₂·p₃`, 그리고 클러스터 합계뿐이다. 종목 상관을
표기하려면 무엇을 "높다" 고 부를지 임계가 필요한데 문서에 그 값이 없다 — 만들지 않았다
(`CLAUDE.md` §1). 위 §2.5 의 "상관 0.7 이상 그룹" 도 같은 이유로 없다.

**앵커 : 토크 줄은 라벨이 있을 때만 비율을 찍는다.** L4 는 2026-08-24 개정 이후 `role` 을
`eligible` 하나로만 내므로(`docs/06` §8.4) 바벨 라벨이 없고, 그때 이 줄은 `라벨 없음 (L4 미지정)`
이 된다 — `0 : 100` 이 아니다. 앵커에 0 을 **배분한 것**과 앵커를 **재지 못한 것**은 다르며,
전자로 읽히면 바벨 붕괴로 오독된다. `diagnostics.json` 의 `anchor_labeled: false` 가 같은 사실이다.

## 7. 열린 질문

### 7.1 `μ_i` 의 기본 선택

(a) 균등이 기본이나, (c) 사이클 진폭이 케이스 스터디로 근거를 얻으면 승격할 수 있다.
승격은 문서화된 근거를 요구한다.

### 7.2 λ (확신도 압축 계수)

0.3 은 선언값. **λ 의 실측 근거는 캘리브레이션 곡선의 기울기이며, 기울기를 재는 방식은
`10-validation.md` §4 이 고정한다** — 그쪽이 정본이므로 유도를 여기에 옮겨 적지 않는다.
갱신은 자동으로 일어나지 않는다: 표본이 `10-validation.md` §4 의 기준선 N ≥ 20 을 넘긴 뒤
**사람이 문서화된 근거와 함께 결정**하고, 그 근거를 커밋 메시지에 남긴다 (`CLAUDE.md` §1).

### 7.3 세금·수수료

현 설계는 무시. 회전율이 낮아(월 단위) 영향이 제한적이나, TP 사다리가 단기 양도소득을
만들면 재검토.

### 7.4 기존 보유 포지션과의 병합

신규 계획이 기존 보유와 충돌할 때의 규칙 (M6).
§5 의 "TP1 후 MDD 예산 해제 → 새 테마 여력" 이 이 질문에 걸려 있다 — 최적화기가 기존 보유를
모르는 한 예산 해제는 일어나지 않는다.

### 7.5 테마 EW 지수의 결측일을 어떻게 다룰 것인가

`risk.monthly_returns`·`risk.index_level` 은 첫 관측 이후의 결측일을 **수익률 0** 으로 메운다.
그 지수가 월간 공분산(C1-i)과 `L_i` 의 과거 낙폭(C1-ii)을 동시에 만들므로, 메운 날은
**변동성과 낙폭을 낮추는 방향**으로 두 제약을 함께 느슨하게 한다. 오차의 방향은 알려져 있고
안전한 쪽이 아니다.

선택지는 셋이다 — (a) 지금처럼 0 으로 메운다, (b) 결측일을 아예 빼고 관측일만으로 복리한다,
(c) 결측 비율이 임계를 넘는 테마를 계산에서 제외한다. **(c) 는 임계를 만들어야 하므로 §1 위반**
이고, (a)↔(b) 는 도메인 근거로 갈리지 배후 데이터로 갈릴 문제가 아니다 (결측이 "거래정지" 면
0 이 맞고 "수집 누락" 이면 틀리다 — 지금 패널은 둘을 구분하지 않는다). **판단을 내리기 전까지
동작은 (a) 로 두고 메운 일수를 세어 표기한다.** 재검토 시점은 L1 패널이 결측 사유를 구분해
적기 시작할 때다.

## 구현 노트 (M6) — `src/msa/l5/` · `msa portfolio`

2026-08-23. 최적화기·사다리·계획서 절반이 서 있다. **케이스 스터디 6건은 없다** (별도 리서치 산출물).
아래는 문서와 코드가 어디서 같고 어디서 다른지, 그리고 코드가 **선언**한 것들이다.

**입력 계약** (`inputs.py` 머리말이 정본). L4·L3 는 임포트하지 않고 파일로만 받는다 — 두 계층이
동시에 만들어지는 동안 결합을 파일 하나로 제한하기 위해서다.

| 파일 | 내용 |
|---|---|
| `<inputs>/picks.csv` | `theme,ticker,role[,entry_price,adv20_usd,rank_score,idio_vol_ann,min_weight,split_first_leg,tp_p50_price,tp_p75_price,prev_cycle_peak_price,notes]` — `role ∈ {anchor,torque,royalty,midstream,etf}` (§06 §5 바벨) |
| `<inputs>/theses/<theme>.yaml` | `thesis.schema.yaml` 의 부분집합 + **`cycle_confidence_source: human|referee`** (누가 `c` 를 만들었는가 — `11-roadmap.md` "M6 구간"). `invalidations` 비면 거부 (`CLAUDE.md` §5) |
| `<inputs>/returns.csv` (선택) | date × ticker 종목 수익률. 있으면 종목 공분산(252행)을 쓴다 |
| `state/cases/cases.yaml` | 케이스 표. 스키마·예시는 `docs/specs/cases.example.yaml`. `L_i` 에 쓰이는 행 = `type: death` · `verified: true` · `sources ≥ 1` · 낙폭 not null, 전부 |

**정식화는 §2 그대로.** 목적 `Σ c̃·μ·w` (μ 방식 (a) 균등), C1-(i) `2.2·‖Σ^{1/2}w‖ ≤ 0.30` (2차 원뿔),
C1-(ii) `Σ w·L ≤ 0.30`, C3 0.15/0.35/0.55, C4 `w·Capital ≤ 0.10·ADV20` (자본을 줬을 때), C5 현금 0.15,
C6 `c ≥ 0.5` 는 풀기 전 필터. `cvxpy` + CLARABEL (설치된 원뿔 솔버 중 CLARABEL → SCS → ECOS 순).

**코드가 선언한 것 — 문서에 없던 결정이라 여기 적는다.**

1. **C1 "보수적인 쪽"** 은 두 제약을 **동시에** 거는 것으로 구현했다. 둘 다 걸면 자동으로 타이트한 쪽이
   구속하고, 풀고 난 뒤 어느 쪽이 경계에 붙었는지를 `mdd_binding ∈ {vol, scenario, both, none}` 로 잰다.
   (ii) 는 `L_i` 가 있는 종목의 **부분합**이다 — 없는 종목은 합에서 빠지고 그 목록이 `scenario_missing`
   으로 나와 경고에 찍힌다. 0 으로 채우지 않는다.
2. **"과거 유사 국면"** (`L_i` 의 첫째 항) = 테마 EW 지수(L1 캐시)가 **누적 고점 대비 −50% 에 처음 도달한
   시점부터 그 고점을 회복할 때까지**의 에피소드. 재는 것은 **진입 시점(−50% 도달일) → 에피소드 최저가**
   의 손실이고 에피소드들의 최대값이다. 진행 중 에피소드도 포함한다. 50% 는 L1 이 테마를 상단에 올리는
   그 조건이며 탐색으로 얻은 값이 아니다. 에피소드가 없으면 이 항은 **없음**이다 (0 아님).
3. **`L_i` 는 두 항이 모두 있을 때만 선다.** 한쪽이라도 없으면 `None` 이고 사유가 남는다. 계획서는
   `L = 0.42 (과거 유사 국면 0.42 / 사망 사례 0.78 × 0.5 = 0.39 [case_id] → 과거 국면이 구속)` 또는
   `L = 계산 불가 (… 사망 사례 —) → C1-(ii) 에서 빠짐 — 사유` 로 찍는다.

   **2026-08-24 갱신.** `state/cases/cases.yaml` 에 4조건(`type: death` · `verified: true` · 출처 ≥1 ·
   낙폭 not null) 을 만족하는 행이 **2건** 있다 — `coal-2013`(낙폭 0.953) · `offshore-drilling-2016`
   (0.99). 둘 다 `clusters: [fossil]` 이므로 **`correlation_cluster: fossil` 인 7개 테마**
   (`coal` · `oil_gas_ep` · `oil_services` · `offshore_drilling` · `refiners` · `midstream` ·
   `intl_majors`) 는 `L_i` 가 실제로 선다. 실측: `coal` 은
   `L = 0.49 (과거 유사 국면 0.09 / 사망 사례 0.99 × 0.5 = 0.49 [offshore-drilling-2016] →
   사망 사례 × 0.5 가 구속)`. **그 밖의 클러스터(예 `nuclear`)는 여전히 매칭이 없어 계산 불가**이며
   사유가 "테마 X (클러스터 Y) 에 해당하는 행 없음" 으로 찍힌다. 즉 C1-(ii) 는 **일부 테마에서만**
   산다 — 전부도 0개도 아니다. (이전 판의 "`verified: true` 사망 사례 행이 0개라서다" 는 stale.)
4. **공분산**: 종목 수익률이 없으면 테마 EW **월간** 수익률 60개월(선언)로 테마 공분산을 만들고 종목을
   자기 테마에 β=1 로 사상한다 (`Σ_stock = BΣ_themeBᵀ + diag(σ²_idio)`, `idio_vol_ann` 없으면 0 →
   같은 테마 종목은 상관 1). 축소는 **상수상관 타깃 δ = 0.5 (선언)** — Ledoit-Wolf 해석적 강도는 sklearn
   의존성을 넣지 않기로 해 쓰지 않았다. 문서의 "252일 Ledoit-Wolf" 와 다르며, 그 차이는 진단의
   `covariance.source/shrink` 에 기록된다.

   **`idio_vol_ann` 결측은 부분 결측도 경고에 뜬다** (2026-08-24). 이전 코드는 "한 종목이라도
   값이 있으면" 경고를 건너뛰어, 나머지 종목이 σ_idio = 0 (테마 내 상관 1) 로 들어간 사실이
   `diagnostics.json` 의 `covariance.notes` 에만 남았다. 지금은 **몇 종목인지 세어** 계획서 경고란에
   찍는다 — `고유분산 0 인 것이 2/3종목 ['UEC', 'PWR']`. L4 는 이 열을 채우지 않으므로
   (배선 W1 표) 실운영에서는 전 종목이 결측인 것이 정상 경로이고, 그것도 경고로 보인다.

   **월 중 결측일은 수익률 0 으로 메운다 — 그 일수를 센다** (2026-08-24). `risk.monthly_returns` 는
   "한 달 전부 NaN" 인 달만 NaN 으로 남기고, 월 중 결측일은 `fillna(0.0)` 으로 곱한다.
   `risk.index_level` 도 같고, **이 지수가 `L_i` 의 "과거 유사 국면" 낙폭을 만든다.** 메운 날은
   무변동으로 들어가므로 변동성과 낙폭이 낮아지는 방향으로 틀린다. **동작은 바꾸지 않았다** —
   보간이든 그 날 제외든 새 결정이고 문서에 선언된 처리가 아니다 (§7.5 열린 질문). 대신
   `risk.filled_gap_days` 가 테마별로 메운 일수를 세어 `diagnostics.json` 의 `filled_gap_days` 와
   계획서 경고란(`테마 EW 수익률에 결측일이 있다 — 수익률 0 으로 메우고 계산했다 (uranium 7일)`)
   에 싣는다 (`CLAUDE.md` §2).
5. **ENB 임계 없음** — §2.4 (b) 그대로. 계획서에 ENB 와 `p₁·p₂·p₃` 를 항상 찍고 경고란에 "눈금 없음" 으로
   다시 적는다. `11-roadmap.md` M6 의 "경고 임계 미달 시 경고 표시" 문구는 이 결정과 충돌해 07 을 따랐다.
6. **완화 순서 C3 → C1**: 0단 전부 → 1단 C3 세 상한 해제 → 2단 예산 0.35/0.40/0.45/0.50 (5%p, 선언) →
   그래도 infeasible 이면 예외. 0.50 너머는 "MDD 30% 감내" 전제와 양립하지 않아 올리지 않는다. 하한
   (`min_weight`·`min_gross`)이 없으면 `w=0` 이 항상 feasible 이므로 완화는 하한이 있을 때만 돈다.
7. **Tier-2 두 규칙** (평단 −35% · 포지션 손실 = 총자본 8%) 은 **먼저 오는 쪽**을 유효 스탑으로 잡고
   어느 규칙인지 적는다. `w ≤ 0.15` 면 8% 규칙은 평단 −53% 라 −35% 가 항상 먼저다 — 완화로 비중이
   0.2286 을 넘을 때만 8% 규칙이 앞선다.

   **산식은 `ladders.tier2_rules(평단, 그 시점 포지션 비중)` 한 곳에만 있다** (2026-08-24). 기준가와
   비중을 무엇으로 놓느냐만 호출자마다 다르고 규칙은 같다 — 계획 단계는 **완납 평단 × 목표비중**,
   `positions.yaml` 제안은 **1단 진입가 × (목표비중 × 1단 비율)**, `msa check` 는 **실제 체결 평단 ×
   체결된 단의 비중 합**. 그래서 하나의 실효 스탑이 계획서 → `weights.csv` → `positions.yaml` →
   `msa check` 로 끊기지 않고 흐른다.

   표시 규약: **가격과 하락률은 언제나 적용된 같은 규칙에서 나온다.** 계획서는 진 규칙을 뒤에
   참고로만 붙이고(§6), `weights.csv` 는 `tier2_price` · `tier2_vs_initial` · **`tier2_rule`** 세 열을
   한 짝으로 쓴다. `positions.yaml` 의 `tier2_basis` 는 `avg_minus_35` 외에 **`capital_8pct`** 를
   가질 수 있고, `msa check` 는 같은 함수로 실효 스탑을 다시 세워 대조하므로 자본 규칙이 이긴 행을
   "평단 −35% 와 다르다" 며 **사람을 오지목하지 않는다**. 회귀 테스트는 두 케이스를 모두 잰다
   (`tests/test_l5_portfolio.py` Tier-2 절 · `tests/test_l5_positions.py`).
   임계 `TIER2_FROM_AVG` 0.35 · `TIER2_CAPITAL_LOSS` 0.08 은 그대로다.
8. **2·3단 발동가는 −13%·−23%** (문서 "−12~15%"·"−22~25%" 의 산술 기준점, §4 표가 쓰는 0.87·0.77).
   TP1 은 `+2R` 가격을 항상 적고 P50 가격은 입력이 있을 때 병기한다. TP2 는 P75·직전 고점 50% 회복 중
   입력이 있는 것을 적는다 — 밸류 백분위 가격은 L4 가 줘야 한다.
9. **자동 주문 없음** (`CLAUDE.md` §8). 산출물은 `state/portfolio/<date>/{weights.csv, plan.md, diagnostics.json}` 이고 `.gitignore` 에 있다.

**남은 것.** (a) 케이스 스터디 6건 → `state/cases/cases.yaml` (`verified: true` + 출처) — 들어오는 순간
C1-(ii) 가 살아난다. (b) `msa picks` → `picks.csv` 배선 (M5 뒤). (c) §7.4 기존 보유 병합 — 미구현.
(d) `× 0.5` 재검토는 (a) 와 같은 시점이다 (§2.4).

**배선 W2 (2026-08-23) — 계획 → `positions.yaml` 제안 (`l5/positions.py`, `msa portfolio --emit-positions`).**
`PortfolioResult.positions` 를 `msa check` 가 읽는 `state_files.Position` 모양으로 옮겨
`state/portfolio/<date>/positions-proposal.yaml` (+ `positions-proposal.md` 승격 체크리스트) 에 쓴다.
**`state/positions.yaml` 은 쓰지 않는다** — 체결 반영은 사람의 일이고(`CLAUDE.md` §8), 행 복사·`status: open`
승격·저널 링크(`thesis_snapshot`·`journal_entry`) 채우기는 체크리스트의 절차다. 사상은 모듈 머리말의 표가 정본이다.
코드가 선언한 것 둘: (i) **`tier2_stop_price` 는 진입가 × (1 − 0.35)** — 1단만 체결된 시점의 평단이 진입가이고
`msa check` 가 "평단 −35% 와 1% 이내" 로 대조하기 때문이다. §4 표의 완납 시 값(초기가 −40.5% 등,
`tier2_effective_price`)은 행의 `note` 에 남기고 사다리가 채워질 때마다 사람이 갱신한다. (ii) **TP 의 기계 가격은
"또는" 조건 중 먼저 오는(낮은) 쪽** (TP1 `min(+2R, P50)` · TP2 `min(P75, 고점 50%)`), 둘 다 없으면 manual.
`role` 은 `anchor`/`royalty`/`midstream` → `anchor`, 그 외 → `torque` (`Pick.is_anchor` 와 같은 묶음, 원 role 은
`note`). `entry_price` 가 없는 종목이 하나라도 있으면 제안 전체를 거부한다 — 가격 없는 사다리는 합칠 수 없다.
테스트 `tests/test_l5_positions.py`.
### 배선 W1 (2026-08-23) — `msa picks` → `picks.csv` · L3 thesis → `theses/*.yaml` (`msa portfolio-inputs`)

위 "남은 것 (b)" 가 `src/msa/pipeline/assemble.py` 로 닫혔다. L5 는 여전히 L4·L3 를 임포트하지 않는다 —
배선은 `msa.pipeline` 패키지만 하고, 산출물은 `state/portfolio_inputs/<asof>/` 디렉터리이며
`msa portfolio --inputs <그 디렉터리>` 가 **바뀐 것 없이** 읽는다. 쓰고 나서 `load_picks`·`load_theses` 로
자기 산출물을 다시 읽어 계약을 확인한다.

```
msa portfolio-inputs --asof 2026-08-14 --themes rare_earth,uranium [--human-theses DIR] [--top N] [--no-write]
```

`state/picks/<date≤asof>/<theme>/ranking.csv` 와 `state/theses/<date≤asof>/<theme>.thesis.yaml` 의 **최신**을
테마별로 고른다 (`--human-theses DIR` 의 `<theme>.yaml` 이 있으면 그것이 우선).

**`picks.csv` — 지금 L4 에서 채울 수 있는 열과 없는 열** (`assemble.OMITTED_COLUMNS` 가 정본, 리포트에 매번 찍힌다)

| 열 | 원천 | 상태 |
|---|---|---|
| `theme` · `ticker` | 디렉터리 · `ranking.csv` index | 쓴다 |
| `role` | `group` — `ANCHOR`→`anchor` · `TORQUE`→`torque` | 쓴다. 라벨이 빈 행(순위만 있음)·모르는 라벨은 **제외하고 센다**. `royalty`·`midstream`·`etf` 는 L4 가 태깅하지 않아 (`06` §8.4) 나오지 않는다 |
| `entry_price` | `price` (asof 이하 마지막 **비조정** 종가) | 쓴다 |
| `adv20_usd` | `adv20_usd` | 쓴다 |
| `rank_score` | `composite` | 쓴다 |
| `notes` | `rank`·`group`·S̃/T̃/M̃·`penalties`·`red_flags`·`composite_partial`·축 입력 결측 | 쓴다 (표기용) |
| `idio_vol_ann` | — | **안 쓴다.** L4 특성 표에 종목 고유 변동성이 없다 → 위 구현 노트 4 의 "없으면 0" 경로 |
| `split_first_leg` | — | **안 쓴다.** §3 "M 축이 낮으면" 의 컷이 문서에 없다 — 컷을 만들지 않는다 (`CLAUDE.md` §1). M̃ 는 `notes` 에 있어 사람이 넣을 수 있다 |
| `tp_p50_price` · `tp_p75_price` | — | **안 쓴다.** 밸류 백분위 회복가는 L4 가 아직 내지 않는다 (구현 노트 8) |
| `prev_cycle_peak_price` | — | **안 쓴다.** L4 특성 표에 없다 |
| `min_weight` | — | **안 쓴다.** L4 산출이 아니다 — 필요하면 사람이 넣는다 (기본 0) |

한 티커는 한 테마에만 — 먼저 온 테마가 갖고 뒤는 제외하고 센다 (`load_picks` 규칙). `--top N` 은 L4 바벨을
더 줄일 때만 쓰고 기본은 바벨 전부다.

**`theses/<theme>.yaml` — thesis 객체의 부분집합.** `theme_id` · `generated_at` · `claim` · `horizon_months` ·
`cycle_confidence` · **`cycle_confidence_source`** · `triggers`/`invalidations` (`observable·source·by·action·status`
만) · `gate_result{status, portfolio_eligible, rule, path}`
· `value_trap_axes.unit_demand{verdict, axis1_available, unit_series_source}` · `assembled_from`(원본 경로).
evidence·bear_case 등 전문은 옮기지 않는다 — `state/theses/`·저널에 있다.

`cycle_confidence_source` 는 **위치로** 정한다 — `state/theses/` 산출은 `referee`, `--human-theses` 디렉터리는
`human`. 단 yaml 이 스스로 `confidence_provenance`(저널 용어) 또는 `cycle_confidence_source` 를 선언했으면
**그 선언이 이긴다** — 파일이 어디 있든 누가 `c` 를 만들었는지는 파일이 더 잘 안다. 리포트에 어느 쪽으로
정했는지(`yaml 선언` / `위치(...)`) 가 찍힌다.

**건너뛰는 테마와 사유** (전부 `report.txt`·`assemble_report.json` 에) — thesis 없음 · thesis 계약 위반
(`parse_thesis` 거부 — 예: `invalidations` 빈 것) · 게이트 편입 불가 (`contested`/`rejected`/`portfolio_eligible:
false`) · picks 없음 · picks 0건(바벨 라벨 없음). 전부 건너뛰면 예외다 (빈 묶음을 만들지 않는다). 실측
(`rare_earth` 2026-08-14): 순위 5 → 바벨 2 (앵커 2) · 나머지 3 은 "바벨 미배정" 으로 셈.
