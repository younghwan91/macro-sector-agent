# 11 · 로드맵

각 마일스톤은 **완료 판정 기준**을 갖는다. 기준을 못 채우면 다음으로 넘어가지 않는다.

## M0 · 설계 (완료 — 2026-08-22)
- [x] 문제 정의 · 계층 구조 · 비목표
- [x] 테마 유니버스 3중 정의 + 109 버킷 초안 + `cycle_class` 분류
- [x] L1 상태벡터 6블록 전 지표 정의
- [x] L2 인과 DAG 스펙 + 드라이버 목록 + 모순 감사 절차
- [x] 가치함정 판별기 5축 + 하드 게이트
- [x] L3 에이전트 계약 + thesis 스키마
- [x] L4 3축 + 바벨
- [x] L5 최적화 정식화 + 사다리 + 2단 스탑 + TP
- [x] 데이터 계약 + 부트스트랩 절차
- [x] 검증 체계 (백테스트 대체 관문)

### M0.1 · 설계 검토 (2026-08-22)

M0 문서 일습을 통독 검토해 **문서가 스스로 세운 규약을 문서가 위반한 자리** 넷을 고쳤다.
전부 문서 수정이며 코드는 여전히 없다.

| 발견 | 자리 | 처리 |
|---|---|---|
| 박스 강건화가 선형 목적에서 의도와 **정반대**(집중화)로 작동 | `07` §2 | 철회 → 평균 보존 압축 `λ` (기본 0.3) |
| Tier2 −35% 의 역산 산술 오류 (평단 −13% 는 2단 진입가를 착각한 값) | `07` §4 | 평단 −8.5%, 초기가 대비 −40.5%, 손실 기여 12.25% 로 정정 |
| `ENB ≥ 3.0` 구속이 **이 저장소의 원형 사례를 배제** (2026 원자재의 ENB ≈ 1.4) | `07` §2 C2 | 구속 해제 → **눈금 없는 리포트 지표**(ENB + 상위 3개 고유값 리스크 기여 `p₁·p₂·p₃`). C1-(ii) 를 1차 방어선으로 승격 |
| 가치함정 축1 이 축2 와 **같은 관측을 반대로 해석** (공급 파괴 → 자동 "사망") | `04` 축1 | 입력 재정의(실물 수요 1순위 · 동일 구성원 폴백) + `axis1_contested` 신설. **임계는 불변** |

곁들여: 캘리브레이션의 선택 편향을 명시하고 **기각 대장**을 신설(`10` §4),
테마 버킷 수가 85 가 아니라 **109** 임을 실측해 저장소 전체를 정정,
축1 적용 가능 테마가 소수라는 사실과 그에 따른 M6 운영 범위를 명시,
케이스 스터디 6건을 **M6 선행 조건**으로 올렸다(C1-(ii) 의 `L_i` 가 거기서 나온다).

> 넷 중 셋이 L5(`07`)에 몰린 것은 우연이 아니다. 그 문서만 **수식을 쓴다.**
> 서술은 틀려도 읽히지만 수식은 틀리면 계산된다 — 구현 전에 잡아야 하는 자리다.

### M0.2 · 백테스트 방침 정정 (2026-08-22)

`~/data/us_micro.duckdb` 에 **1997-12-31~2026-08-14 · 20,931 종목 · 폐지 18,169개 포함**
스토어가 실재하는 것을 확인하고, "백테스트를 하지 않는다" 를 **"절반은 한다"** 로 정정했다.

이전 판이 든 세 이유 중 **재구성 불가만 옳고, 그것은 L3 에만 해당한다.** 표본 부족과
비정상성은 검정을 어렵게 만들지 불가능하게 만들지 않으며, 어려움은 유효 표본을 줄여 잡는
것으로 다룬다. **하지 않는 것과 할 수 없는 것을 구분하지 않은 것**이 이전 판의 잘못이다.

| 바뀐 것 | 자리 |
|---|---|
| L1·L2·L4 는 백테스트하고 walk-forward·DSR·PBO 관문을 붙인다 | `10` §1·§2, 새 마일스톤 M3.5 |
| **검정 결과로 값을 바꾸는 금지는 유지** — 오히려 IC 라는 눈금이 생겨 위험이 커졌다 | `10` §2.4·§8, `CLAUDE.md` §1 |
| "백테스트를 하지 않으므로 look-ahead 가 성립하지 않는다" 는 **틀렸다** | `CLAUDE.md` PIT 규약 — 경로를 둘로 나눴다 |
| 성과 광고 금지의 근거가 "백테스트가 없어서" → "**전략 수익률이 만들어지지 않아서**" | `CLAUDE.md` §7 |

> 이 정정이 최초 설계 검토의 미해결 지적 하나를 푼다 — L1 블록 가중치 48개와 L2 엣지 강도가
> **영원히 반증 불가**였던 것이, 이제 절반은 반증 가능하다. 금지가 오버피팅은 막지만
> **선언이 틀렸는지는 알려주지 않는다.**

**다음 행동**: M1 착수 (Sharadar 스토어가 있는 머신에서).

---

## M1 · 데이터 부트스트랩
`08-data-contract.md` §6 절차 실행.

**완료 기준**
- [x] Sharadar 스토어 접속, 행수·기간이 벌크 원본과 일치 (조용한 절단 없음) — `msa data status` · `src/msa/data/store.py` (M1)
- [x] `ACTIONS` · `TICKERS.industry` 적재. `SFP` 는 스토어에 없어 벌크 `funds.csv.zip` 을 직접 읽는다 (`etf_prices`, M1 실측)
- [ ] FRED 시리즈 **24종** 적재 + **발표 지연 실측 표** 작성 (§3 "확인 필요" 3건 해소) — 어댑터·`msa data fred-lag` 는 있으나 **`FRED_API_KEY` 가 없어 실측 미완**
- [ ] `hyperscaler_capex` 파생값이 5사 공개 capex 와 일치
- [x] `msa data status` 가 전 소스의 최신 시점과 결측률을 출력

## M2 · 테마 유니버스 확정
**완료 기준**
- [x] `state/themes.yaml` 버킷 확정 — **134개** (초안 109 − 폐기 6 + 신설 31, `docs/theme-coverage-audit.md`)
- [x] 커버리지 감사 6항목 — 1·2 PASS, 3·4 경고 표기, 5 는 M3 에서 실행(17/44 > 0.85), 6 은 기준선 확보 (`docs/theme-coverage-audit.md`)
- [x] 폐지 종목이 자기이력 구간에 포함됨을 검증 — 배정 17,917 중 폐지 다수 포함, 큐레이션 4개 버킷에 잔존 편향 기록
- [x] ETF 프록시 vs 자체지수 12M 상관 > 0.85 인 버킷 비율 리포트 — **17/44 (38.6%)**, 중앙값 0.805. 매 스캔 `coverage.csv` 에 재계산 (M3)
- [x] **`physical_ref`(실물/가격 참조) 보유 버킷 수를 집계해 문서화** — 45/134. M3 에서 `kind`(price 19 · volume 25 · nominal 1) 추가 — 이 수가 곧
      가치함정 축 1(물량 추세)을 계산할 수 있는 테마의 수이고, 그것이 **M6 시점의 실제 운영 범위**다
      (`04-value-trap.md` 축 1, 아래 "첫 실전 사용 시점"). M6 에서 알면 늦으므로 M2 에서 센다
- [ ] `01-theme-universe.md` §6 의 열린 질문 4건 결정 및 문서 반영

## M3 · L1 사이클 스캐너
**완료 기준**
- [x] 6블록 전 지표 계산, M2 에서 확정된 전 버킷 스코어보드 산출 — `msa scan`, `src/msa/l1/` (2026-08-23)
- [x] `momentum` 의 VCP 를 **지수 레벨로 승격**한 래퍼 동작 — `vendor/vcp.py` + `blocks.vcp_index_score`.
      Stage 분석은 `above_200`·`sma200_slope`·`breadth_200` 조합으로 대체 (별도 Stage 래퍼는 만들지 않았다)
- [x] 브레드스 3종(`breadth_200`·`nh6m`·`nhnl`)과 `breadth_lead` 산출
- [x] 적자 기업 제외 비율이 리포트에 표기됨 (`ebitda_nonpos_share`, 2026-08 중앙값 20%)
- [x] 소표본·짧은이력 버킷에 경고 플래그 (`n=N 소표본` · `short_hist`)
- [x] **육안 검증** — **어긋났다. 기록하고 넘어간다.** `docs/scan-m3-check.md`. 2026-08-14 시점 원자재
      계열은 중·하위다 (2026-02 고점 후 −40% 급락이 "패닉" 이지 "망각" 이 아니라는 A 블록의 설계와
      일치). 2025-03·06 시점으로 되감아도 원형 사례(`silver_miners`·`pgm_miners`·`rare_earth`·`lithium`)는
      44~90위로 상단에 없었다 — E 블록(자본사이클)이 당시 광업 capex 확장기라 낮았다.
      **가중치는 건드리지 않는다** (`CLAUDE.md` §1). 이것이 M3.5 가 정량으로 답해야 할 질문이다

### M3 구현 메모 (2026-08-23)
- 코드: `src/msa/themes.py`(로더·배정) · `src/msa/l1/{panel,fundamentals,physical,blocks,scoreboard,scan}.py`
  · `src/msa/vendor/{taa_signals,vcp}.py`. 테스트 `tests/test_themes.py`·`test_l1_*.py` (합성 데이터).
- 실행 시간: 패널 44초(DuckDB 1패스, 캐시) · 재무 22초 · 지표+VCP 약 40초 → 전체 1분 30초, 캐시 후 수 초.
- 데이터 한계를 리포트에 적는다: `FRED_API_KEY` 없음 → CPI·FRED 참조 19개 없음 (`dd_real` 미계산),
  manual 참조 21개는 `state/physical/manual/<SYMBOL>.csv` 가 없어 `data_missing`. 축 1 데이터 보유는
  **선언 45 중 7** (ETF 가격 참조). `estimates` 0행 → `surprise_dir`·`guidance_rev` 미계산.
- 문서가 정하지 않은 구현 결정은 `02-cycle-state.md` §10 에 모았다.

## M3.5 · L1 백테스트 (관문 0)

> **M5·M6 앞에 온다.** 이 검정이 답하는 것은 "가중치를 어떻게 고칠까" 가 아니라
> **"스코어보드를 순위 산출에 쓸 것인가"** 다. 답이 아니오면 뒤 단계의 전제가 무너진다.
> `10-validation.md` §2 가 정본이다.

**완료 기준** (2026-08-23 실행 · 판정 문서 `docs/backtest-l1.md` · 코드 `src/msa/l1/backtest.py` · `msa backtest l1`)
- [x] 폐지 종목을 포함한 테마 지수를 1998년까지 소급 구성 (생존 편향 없음을 검증) — M3 의 `panel.py` 그대로
      (1997-12 ~ 2026-08, 폐지 종목 포함 소급). 백테스트는 그 EW 지수의 월말 값을 쓴다
- [x] 월말 `cycle_score` 와 이후 3·6·12M 초과수익의 **횡단면 rank-IC** 시계열 산출 —
      `ic_timeseries.csv`. **주 창(2011–) 12M 복합 IC +0.048 [−0.024, +0.123] → 0 과 구분되지 않는다 (FAIL).**
      전 구간은 세 호라이즌 하한이 0 을 간신히 넘는다 (+0.001~+0.017)
- [x] 상위 K=8 vs 하위 8 분위 스프레드 (실제 운영 컷오프에서) — 주 창 12M +5.8%p [−2.4, +15.5], 0 포함
- [x] **블록별 단독 IC** A~F 6종 + `cycle_class` 별 분해 — **C 만 뚜렷이 일한다(+0.10~+0.13); A·D 는 반대
      부호(−0.08~−0.14 / −0.04~−0.07); E 약하게 양(+); B·F 0.** `02` §7 의 주장 중 `commodity_supply` E 0.30
      (E 는 0 과 구분 안 됨, C 가 가장 셈) · `capex_program` F 0.35 (F 음수) · `credit_rate`·`secular_growth`
      D (0) 가 어긋났다. `policy_program` C 0.30 은 일치. **가중치는 건드리지 않았다**
- [x] `breadth_lead` 의 실측 선행 개월 수 — 지수 SMA200 상향 전환 3,329 에피소드 중 브레드스가 먼저 돈 것
      14.8% (리드 중앙값 2M), 같은 달 45.9%, 브레드스가 늦은 것 35.9%. "먼저 돈다" 는 이 정의로는 일반 명제가 아니다
- [x] **유효 표본 축소**: 월별 IC AR1(12M 0.90 → N_eff 8.8) · 테마 간 평균 상관(원 0.50 / SPY 초과 0.19) ·
      12개월 블록 부트스트랩 신뢰구간 (모든 CI)
- [x] **DSR 시도 수를 명시적으로 세어 기록** — 608 (규칙은 `backtest.count_trials`). 복합 12M 주 창 DSR N=1 0.87 /
      N=608 0.02. PBO (월별, S=16, 전략 = 복합+블록 6) 0.003~0.10 — 변형 간 순위가 안정적이라는 뜻이지 복합이 좋다는
      뜻이 아니다
- [x] 판정을 `docs/backtest-l1.md` 에 기록 — **어긋난 것도 그대로 적었다.** 가중치·방향·임계를 옮기지 않았음은 이
      커밋의 diff 가 증명한다 (`src/msa/themes.py` `BLOCK_WEIGHTS` · `scoreboard.py` `ORIENTATION`·`SCORED` 무변경)

**판정의 결과.** `10-validation.md` §2.3 첫 행에 해당한다 — 복합 `score` 의 순위를 상위 K 선정 근거로 쓰는
것은 정당화되지 않는다 (관찰 지표로 강등, README 에 적음). M5·M6 는 이 전제 위에서 입력을 다시 정한다 —
무엇을 받아 돌지는 이 문서가 아니라 다음 설계 결정이 정한다 (`CLAUDE.md` §1: 이 표로 가중치를 만들지 않는다).

## M3.6 · 설계 질문 1 — A(망각)의 집계 방식 (사전 등록된 검정) — 실행 완료 2026-08-23

> M3.5 의 판정(FAIL)이 가리킨 것은 가중치가 아니라 **집계 구조**일 수 있다 — `docs/02` 가 A 를 조건으로
> 서술하면서 §7 에서 가산한다. 질문과 절차는 `docs/12-design-question-a-block.md` §4 에 **실행 전에** 고정했고,
> 그대로 돌렸다. 결과는 `docs/backtest-l1.md` §12 · `docs/12` §7.

**완료 기준**
- [x] `docs/12` §4.1 의 후보 S1(절대 게이트)·S2(풀/타이밍 2단) 를 **정의 그대로** 구현 — `src/msa/l1/structures.py`
      (현행 S0 은 건드리지 않음; 같은 블록 백분위·같은 클래스 가중치, 집계만 다름)
- [x] `msa backtest l1-structures` 로 같은 관문(주 창·12M·CI 하한 > 0) 적용, S1 의 G(t) < 20 인 달 제외 수 보고 (120/175)
- [x] 시도 수 632 로 DSR 정산, `docs/backtest-l1.md` 에 **새 절(§12)** 로 기록 (기존 절 불변)
- [x] 결과: **S1 FAIL · S2 PASS** (주 창 12M +0.078 [+0.015, +0.145]; 단 DSR(632) 0.003 · 12M 스프레드 0 포함 ·
      `commodity_supply` 에서 0 과 구분 안 됨 — 함께 기록)
- [x] **채택 — 2026-08-23 사용자 결정.** `02` §7.1 개정 + `scoreboard.py` (`aggregate_scores`: pool=mean(A,B)≥0.5
      자격 · score=C·E·F 재정규화 · `score_s0` 감사용) + README + `journal/2026-08-23-l1-structure-s2-adoption.md`
      를 한 커밋으로. 이후 `msa backtest l1` 의 `score` 는 S2 를 잰다 (S0 기록은 `backtest-l1.md` §0–§11)
- [x] S2′(D 를 풀 점수에) 등 새 후보는 돌리지 않았다 — 새 사전 등록 대상

## M4 · L2 거시 DAG
**완료 기준**
- [ ] `state/macro-dag.yaml` 작성 — **개수가 아니라 조건이 기준이다: 전 테마가 최소 2개의
      입력 엣지를 갖는다.** 확정 버킷 수 N 에 대해 엣지 수의 하한은 2N 이고, 초안 109개
      기준이면 218 이다 (상한은 없다 — `channel` 을 쓸 수 있는 만큼). 공통 인자 엣지는
      이 하한에 세지 않는다 (전 테마에 같은 부호라 테마를 고르는 데 쓸모없다 —
      `03-macro-dag.md` §7). 이전 판의 "~150개" 는 버킷 85개 기준으로 잡힌 수치였다
      — **완료**: 드라이버 26 · 엣지 레코드 86(개별 83 + 공통 3) · 테마-엣지 쌍 451 / 하한 268 ·
      in-degree min 2 · median 3 · max 6 — **134 버킷 기준 재감사 통과 (M4-134)**. 초안 기준
      수치(쌍 380)는 폐기 id 6개와 신설 버킷 31개의 in-degree 0 으로 22건 실패했고, 재매핑 +
      엣지 14개 추가로 해소했다 (`docs/macro-dag-audit.md` §10)
- [x] `channel` 없는 엣지 0개 (스키마 검증) — `scripts/audit_dag.py`
- [x] `tailwind` 계산 + 공통 인자 횡단면 중앙값 차감 — `src/msa/l2/tailwind.py`, `msa macro`
      (`03-macro-dag.md` §8.3). 단, **오늘 실제 값은 3개 드라이버(GLD·CPER·hyperscaler_capex)로만**
      계산된다 — FRED 23개 드라이버가 `FRED_API_KEY` 없음으로 결측
- [x] 국면 4분면 시각화 — ASCII 차트 + 24개월 CSV (`regime.txt`·`regime.csv`, §8.4). PNG 없음.
      **오늘은 계산 불가** (성장·인플레 축 구성 드라이버 전부 FRED)
- [x] 모순 감사 1회 실행, 플래그 목록 리뷰 — `docs/macro-dag-audit.md` (`contradicts_when` 26개).
      런타임 평가(`contradictions.csv`)는 기계 규칙(`contradicts_rule`)이 있는 2개만 판정하고
      나머지는 `PROSE_ONLY` 로 표에 올린다
- [ ] **엣지 부호 일치율 실측** (`10-validation.md` §2.1) — 선언한 `sign` 과
      36·60개월 창 상관의 부호가 맞는 비율. **불일치 엣지를 고치는 것이 아니라 세는 것**이다.
      기계는 있다 (`src/msa/l2/signcheck.py`, `docs/macro-dag-sign-check.md` 는 실행 불가 기록).
      **실측은 안 됐다** — 451쌍 중 29쌍(ETF·Sharadar 드라이버)만 계산, FRED·수동 기반 422쌍은 키/CSV 대기

## M5 · L4 종목 선정
**완료 기준**
- [x] S·T·M 3축 + 하드 제외 필터 — `src/msa/l4/` (`features`·`axes`·`barbell`·`picks`), `msa picks`.
      **입력이 없어 적용하지 못한 것**: `going_concern`(감사의견 — 스토어에 없음),
      `maturity_wall_24m`(SF1 에 만기 스케줄 없음 → `debtc`/시총 12개월 대용). 둘 다 리포트와
      `meta.json` 에 매번 적힌다 (`06-stock-selection.md` §8)
- [x] `fin-checkup` 레드플래그 이식 동작 — `src/msa/vendor/redflags.py`. 4종 계산
      (완전자본잠식·3년 연속 영업손실·흑자인데 영업현금 유출·3년 연속 이자보상 < 1),
      `partial_capital_impairment` 는 SF1 에 자본금이 없어 **계산 불가**로 표기
- [x] 앵커/토크 바벨 분류 — `barbell.classify`. 앵커 수 비중을 리포트에 명시
- [x] **사후 대조** 실행 — `docs/picks-m5-check.md`. **결과는 그대로 적었고 값을 바꾸지 않았다**:
      MP 는 `rare_earth` 토크 축 **T̃ 1.00 (1위)** — 다만 T 를 계산할 수 있는 구성원이 2개(MP·USAR)뿐이고,
      바벨 규칙("S̃ 상위에서 T̃ 최고" 를 앵커가 먼저 집는다)상 **앵커**로 분류된다.
      AG 는 `silver_miners` 토크 축 **T̃ 1.00 (1위)** — 역시 계산 가능 2개(AG·SVM), 앵커로 분류.
      2026-08 시점 AG 의 `margin_headroom` 은 −22pp (마진이 이미 테마 P75 위) — 토크를 끌어올린 것은
      `price_beta_hist` 1.21·opleverage 다. SBSW 는 SF1 미수록(20-F)이라 **판정 불가**, ALM 은
      `rare_earth` 에서 S̃ 1.00 이지만 분기 6개뿐이라 T 계산 불가. 표본이 2개인 "1위" 는 검정이
      아니다 — **M3.5 와 같은 종류의 관문이며, 맞지 않는 것을 맞게 고치지 않았다** (`CLAUDE.md` §1)
- [ ] 로열티/스트리밍·미드스트림 옵션 그룹 태깅 (§5) — Sharadar 라벨이 없어 미구현
- [ ] L4 스코어 백테스트 (축별 rank-IC) — `10-validation.md` §2 경로. 특성 함수는 `asof` 만
      바꾸면 PIT 로 돈다 (`features.build_features`)

## M6 · L5 포트 구성기
> **구현 상태 (2026-08-23)** — 최적화기·사다리·계획서 절반은 `src/msa/l5/` · `msa portfolio` 로 서 있다
> (`07-portfolio.md` "구현 노트 (M6)"). **케이스 스터디 6건은 2026-08-23 작성됐다** (`docs/cases/`, `state/cases/cases.yaml`) —
> `L_i` 에 쓸 수 있는 행은 coal-2013 · offshore-drilling-2016 둘이라 C1-(ii) 는 `coal`·`offshore_drilling`
> (및 같은 클러스터 `fossil` 의 테마)에서만 숫자를 만들고, 나머지는 `L = 계산 불가 (… 사망 사례 —)` 로 찍힌다.

**완료 기준**
- [x] SOCP 풀이 (ENB 는 구속 제약이 아니다 — 클러스터 상한 반복은 사용자가 요구할 때만 도는 선택적 절차)
      — `cvxpy` + CLARABEL. 클러스터 상한은 `--cluster-cap name=cap` 로만 들어간다 (`optimize.py`)
- [x] MDD 예산 두 방식(변동성·시나리오) 병행 계산, 보수적 쪽 채택 — 두 제약을 **동시에** 걸어
      자동으로 타이트한 쪽이 구속하고, 어느 쪽이 경계에 붙었는지(`mdd_binding`)를 계획서에 적는다.
      단, (ii) 는 `L_i` 가 있는 종목의 **부분합**이며 빠진 종목은 경고로 드러난다
- [x] **케이스 스터디 6건 작성** (`04-value-trap.md` §5 · 사이클 3 · 사망 3) —
      **M6 의 선행 조건이다.** C1-(ii) 가 1차 방어선이 되면서 시나리오 손실 `L_i` 가
      사망 사례의 낙폭을 입력으로 요구한다 (`07-portfolio.md` §2 C1).
      → 2026-08-23 작성: `docs/cases/` 6건 + `state/cases/cases.yaml` + `docs/cases/README.md`.
      6건 모두 게이트 결과가 역사와 일치했다. `verified: false` 3건
      (silver-miners-2015 축1 10y 출처 없음 · tankers-2021 발주잔량 출처 본문 미확인 ·
      mall-reit-2018 축1 이 저장소 설정상 n/a). offshore-drilling-2016 은 쓴 값이 전부 출처 있어 true
      (리그 10y 시계열은 쓰지 않은 대안 입력). `L_i` 사용 가능 행(`load_cases` 기준):
      **coal-2013 0.953 · offshore-drilling-2016 0.990**. reit_retail 버킷 0.640 은 미검증이라 불가
      (몰 REIT 개별 −77~−100% — 어느 쪽을 쓸지 `07` 리포트가 명시해야 한다).
      `× 0.5` 계수 재검토는 이 작업에 포함하지 않았다.
- [ ] **`L_i` 를 케이스 스터디에서 산출했음을 검증** — 각 테마의 `L_i` 가
      `max(과거 유사 국면 최대 낙폭, 케이스 스터디 사망 사례 낙폭 × 0.5)` 로 계산되고,
      어느 케이스에서 왔는지 리포트에 출처로 표기된다. 케이스가 없는 테마는
      `L_i` 를 만들 수 없으므로 C1-(ii) 를 계산할 수 없다는 사실이 드러나야 한다
      (이 사용은 임계값 조정이 아니라 **손실 크기의 추정**이다 — `10-validation.md` §7)
      — **기계 쪽은 있다**: 공식·출처 표기(`[case_id]`)·"없음" 표기는 `risk.py` 가 하고 테스트가 잰다.
      **입력 쪽이 없다**: 표 `state/cases/cases.yaml` 의 스키마와 예시(`docs/specs/cases.example.yaml`,
      11행 전부 `verified: false` · `sources: []`)에 더해 이제 `state/cases/cases.yaml` 에
      `verified: true` 사망 행 2개(coal-2013 · offshore-drilling-2016)가 있다 — `coal`·`offshore_drilling`
      (·클러스터 `fossil`)에서 `L_i` 가 설 수 있다. 체크는 실제 계획서에서 출처 표기를 확인한 뒤에 한다
- [x] 사다리 3단 · Tier1/2 스탑 · 시간 스탑 · TP 3단 산출 — `ladders.py`. M0.1 정정 산술
      (50/30/20 → 평단 −8.5% · Tier2 = 초기가 −40.5% · 손실 기여 12.25%)을 테스트가 재현한다
- [ ] **`cycle_confidence` 수기 산출 경로가 서 있다** — 아래 "M6 구간에 `c` 를 누가 만드는가"
      의 절차대로 사람이 `04-value-trap.md` §4 규칙을 적용하고, 그 값과 5축 판정·증거가
      결정 저널 진입 항목에 남으며(`09-operations.md` §2), 스캔 산출물에도 같은 값이 기록된다.
      `c` 없이는 목적함수도 C6 도 사다리도 계산되지 않으므로 이것이 M6 의 실행 전제다
- [x] **확신도 압축 `c̃_t = c̄ + (1−λ)(c_t − c̄)` 를 목적함수에 적용하고 쓰인 `λ` 를
      매매계획서에 표기** (기본 λ=0.3 선언값, `07-portfolio.md` §2·§6) — 계획서 머리 `확신도 압축 λ = 0.3`
- [x] `07-portfolio.md` §6 형식의 매매계획서 출력 — `plan.py` → `state/portfolio/<date>/plan.md`
- [x] **ENB 를 리포트 지표로 산출하고 표기** (집중을 막지는 않는다) — ENB 와 `p₁·p₂·p₃` 를
      항상 찍는다. **경고 임계는 두지 않았다** — 이 항목의 이전 문구("경고 임계 미달 시 경고 표시")는
      `07-portfolio.md` §2.4 (b) 가 임계를 없애기로 한 결정과 충돌하므로 07 을 따른다
- [x] **축 1 적용 가능 테마 목록(`physical_ref` 보유)을 산출하고 리포트에 표기** —
      후보 테마별 가능/불가 목록 + 유니버스 45/134 를 계획서에 적고, 불가 테마가 편입되면 경고를 낸다
- [x] infeasible 시 완화 순서 고정 동작 (**C3 → C1**. C2 는 구속 제약이 아니므로
      완화 대상이 아니다 — `07-portfolio.md` §2 풀이 · `09-operations.md` §5) — 0단 전부 →
      1단 C3 해제 → 2단 예산 0.35/0.40/0.45/0.50 → 그래도 안 되면 예외. 하한(`min_weight`)이
      없으면 `w=0` 이 항상 feasible 이라 완화는 하한이 있을 때만 돈다
- [ ] **L4 → L5 배선** — `msa picks` 산출물을 `picks.csv` 계약으로 떨어뜨리는 연결 (M5 가 끝난 뒤).
      계약은 `src/msa/l5/inputs.py` 머리말이 정본이다

## M7 · L3 에이전트
> **의도적으로 늦다.** 결정론적 계층이 먼저 후보를 좁혀야 비용과 편향이 통제된다.

**완료 기준** (2026-08-23 — 프레임워크 구현 `src/msa/l3/`, `msa research <theme>`. 구현 노트는 `05-agent-research.md` §7)
- [x] 4역할(supply · catalyst · bear · referee) 구현 — 프롬프트·출력 스키마·오케스트레이션.
      **실제 LLM 호출은 미검증**: 런타임에 `ANTHROPIC_API_KEY` 와 웹 검색 도구가 없어 Mock/Fixture 제공자로만 돌렸다
- [x] thesis 스키마 검증 (evidence·invalidations 없으면 거부) — `05` §4 규약 전 행이 검증기·테스트
- [x] `bear` 가 **L1 스코어를 못 보는** 독립 컨텍스트로 실행 — `BearInputs` 계약 + 프롬프트 토큰 전수 검사 테스트
- [x] 하드 게이트 자동 적용 + 기각 사유 기록 — `04` §3·§3.1 기계 적용, 기각도 저장, `rejections-pending.yaml` 행
- [x] 재실행 시 이전 thesis diff 출력 — 필드별 diff + 무효화 제거 의심 표시
- [ ] **M6 에서 작성된 케이스 스터디 6건을 에이전트 프롬프트에 few-shot 으로 투입**
      (작성은 M6, 투입은 M7 — 두 일은 다르다). 로더(`state/cases/*.md`)는 연결돼 있고 파일이 없으면
      프롬프트에 "few-shot 없음" 을 적는다 — **케이스 파일이 아직 없어 미완료**

## M8 · 운영
**완료 기준**
- [x] 월간/주간/일간 케이던스 스케줄러 — `msa ops schedule --print-cron` (crontab · systemd 텍스트 생성,
      설치는 사람이) + `msa ops due <cadence>` 1영업일 게이트. 미국 공휴일 미고려(문서화됨)
- [x] 결정 저널 append-only 강제 (수정 시도 시 거부) — `msa journal verify` (HEAD 대비 수정·삭제 검출),
      `scripts/journal-precommit.sh` + `msa journal install-hook`. 항목 6종은 필수 필드가 비면 작성 거부
      (`msa journal new --from`), 기존 파일은 덮어쓰지 않는다, `msa journal diff <theme>` 논지 표류 diff
- [x] 텔레그램 알림 6종 — `msa.ops.alerts.AlertKind` (09 §3 의 6행) + `tier2_stop_hit` 1종 추가(07 §4).
      문구 규약은 테스트가 강제한다. 토큰·chat_id 둘 다 없으면 "not configured" (파일 `alerts.json` 은 항상)
- [x] `state/scans/` 스냅샷 보존 + 과거 스캔 재현 가능 — `msa ops reproduce <date>` 가 저장 파일만으로
      리포트를 재생성해 보관본과 대조한다 (2026-08-14 스냅샷: 동일. 2026-07-31 은 `bucket` 키 이전
      렌더러 산출이라 헤더 1줄·CPI 1줄이 다르다고 **보고**된다)
- [x] 캘리브레이션 집계 스크립트 — `msa ops calibration` (표본 = 저널 청산 항목. N<20 → "결론 없음 (N=…)"
      + 표본 나열. N ≥ 20 이고 3개 이상 구간이면 `10-validation.md` §4 가 고정한 **구간 표본수 가중
      최소자승 기울기** → λ 실측 근거. 갱신은 사람이. 조건부 캘리브레이션 문장이 항상 붙는다)
- [x] **기각 대장 집계** — `msa ops rejections-update` (테마 EW 지수로 `r_12m`/`r_24m` 기계 갱신,
      불변 규칙 위반 시 저장 거부, 세 질문 (a)(b)(c) 를 `state/rejections-summary.md` 에 — (c) 는
      `state/scans/<date>/scoreboard.csv` 순위 스냅샷, (b) 는 지표 캐시의 사후 축 1 판정)
- [x] **연결 — 배선** (2026-08-23 W1~W4) — L4 picks + thesis → L5 입력 묶음(`msa portfolio-inputs`, W1) ·
      L5 → `positions-proposal.yaml` 제안(W2; `positions.yaml` 은 설계대로 사람이 쓴다) · L3 라운드 →
      저널 기각 항목·`rejections.yaml`·관찰 목록·진입 초안(`msa ops ingest-theses`, W3) ·
      **월간/주간 배선 — cron 이 `msa run monthly`/`msa run weekly` 를 호출**(W4: 스캔→거시→상위 K→L3→
      적재→L4→L5 를 한 명령으로, 단계별 ok/skipped/unavailable/failed 리포트 `state/runs/<date>/`)
- [ ] **연결 — 실전** — 키 없이는 L3 가 돌지 않아 `msa run monthly` 의 기본이 `--provider none`(사람 논지/
      직전 thesis 만 찾는다)이고, 실제 L3 라운드(`--provider anthropic`)로 월간을 끝까지 돌려 본 적이 없다.
      `monthly_summary` 텔레그램도 리포트 파일까지만 (`09` §1 배선 노트 · 구현 노트 "케이던스 오케스트레이터")
- [ ] `msa check` 실전 검증 — 실제 포지션으로 한 분기 이상 돌려 manual 비율·오탐을 본다

---

## 순서를 바꾸면 안 되는 네 곳

1. **M2 가 M3 앞** — 테마 정의가 틀리면 스코어는 계산돼도 의미가 없다.
   커버리지 감사를 통과하지 않은 채 스캐너를 만들면 몇 달 뒤 전부 다시 짠다.
2. **M7 이 M5·M6 뒤** — 에이전트를 먼저 붙이면 "그럴듯한 논지" 가 파이프라인 전체를
   지배한다. 결정론적 계층이 먼저 서 있어야 LLM 이 좁은 허리에 머문다.
3. **케이스 스터디 6건이 M6 앞** — `07-portfolio.md` §2 에서 ENB 구속을 빼고 `c` 의 박스
   강건화를 철회하면서, 상관 집중 위험을 실제로 가격에 반영하는 물건이 `σ_p` 와
   C1-(ii) 시나리오 손실 둘만 남았다. 그중 스트레스 상관 1 수렴을 직접 모델링하는
   C1-(ii) 가 **1차 방어선**이고, 그 입력 `L_i` 는 사망 사례의 낙폭에서 나온다.
   케이스가 없으면 (ii) 는 숫자를 만들 수 없고, MDD 예산은 변동성 한 축으로만
   지켜진다 — 그것이 과소평가한다는 것이 애초에 (ii) 를 둔 이유다.
   이전 판은 이 6건을 M7 완료 기준에 두었는데, M6 가 M7 의 산출물을 필요로 하는
   순서 모순이었다. 작성은 M6, few-shot 투입은 M7 이다.

4. **M3.5(백테스트)가 M5·M6 앞** — 이 검정은 튜닝 루프가 아니라 **스코어보드를 쓸지 말지**를
   정하는 관문이다 (`10-validation.md` §2.3). IC 가 0 과 구분되지 않으면 L1 은 순위 산출에서
   내려가고, 그러면 L4·L5 는 무엇을 받아 돌지가 달라진다. 뒤에 돌리면 이미 지은 것을 다시 짓는다.

## 첫 실전 사용 시점 — 범위를 좁혀서 시작한다

M6 완료 시점부터 **에이전트 없이** 운영 가능하다. 다만 **운영 범위가 전 테마가 아니다.**

가치함정 판별의 가장 결정적인 축은 축 1(물량 추세)인데, 이 축은 실물 수요 시계열이나
가격지수(`physical_ref`)가 있어야 계산된다. **M2 실측 결과 확정 134개 중 45개**다
(설계 단계 추정 20여 개보다 두 배 이상). 나머지 89개는 축 1 을 못 쓰므로
판별의 무게가 축 3(대체 위협)으로 넘어가는데, 축 3 은 LLM 영역이고 **L3 에이전트는 M7 이다.**
사람이 매달 80~90개 테마의 대체 위협을 직접 판정할 수는 없다.

> 따라서 **M6 시점의 운영 범위는 축 1 을 계산할 수 있는 테마로 사실상 한정된다.**
> 이것은 결함이 아니라 **의도된 단계적 축소**다. 그 범위가 정확히 이 저장소의 원형 사례가
> 나온 곳이다 — 2026년 원자재(AG·SBSW·MP·ALM)는 전부 `physical_ref` 를 가진 테마다.

나머지 테마는 **M7 까지 관찰 목록에 머문다** — L1 스코어보드에는 계속 뜨고 순위도 매겨지지만,
축 1 없이 매수 판정을 내리지 않는다. 리포트는 각 테마가 어느 쪽인지 표기한다(M6 완료 기준).
M7 은 그 판별을 넓고 빠르게 만드는 증폭기이며, **관찰 목록을 실전 범위로 바꾸는 것**이 그 역할이다.

### M6 구간에 `c` 를 누가 만드는가 — 사람이 만든다

`c` 없이는 M6 이 돌지 않는다. 목적함수의 기대수익 가중치, C6 최소 확신도 0.5,
확신도 연동 사다리가 전부 `cycle_confidence` 를 입력으로 받는다 (`07-portfolio.md` §2·§3).
그런데 그 값을 산출하는 주체로 `04-value-trap.md` §4 가 지목한 것은 `referee` 에이전트이고,
**`referee` 는 M7 이다.** 즉 M6 구간에는 산출 주체가 비어 있다. 그 자리를 이렇게 메운다.

| 항목 | M6 구간 (에이전트 없음) | M7 이후 |
|---|---|---|
| 산출 주체 | **사람** | `referee` 에이전트 |
| 적용 규칙 | `04-value-trap.md` §4 가감점 규칙 | 좌동 (같은 규칙) |
| 규칙 밖 조정 | **금지.** 필요하면 `key_uncertainties` 에 서술 | 좌동 |
| 증거 요구 | **완화 없음** — `evidence` 배열 필수 (`CLAUDE.md` §3) | 좌동 |
| 기록 위치 | 결정 저널 진입 항목 (`09-operations.md` §2) | 좌동 |
| 캘리브레이션 | `10-validation.md` §4 표본에 **포함** | 좌동 |

규정:

1. **사람이 `04` §4 의 가감점 규칙을 기계적으로 적용한다.** `base = 0.5` 에서 시작해
   해당하는 항만 더하고 빼고 [0,1] 로 클립한다. 규칙 밖의 조정은 하지 않는다 —
   조정하고 싶은 사유가 생기면 값을 움직이는 대신 `key_uncertainties` 에 서술한다.
   **에이전트에게 거는 제약을 사람에게도 그대로 건다.** 그러지 않으면 M7 에서 에이전트로
   교체하는 순간 두 산출이 비교 불가능해진다.
2. **증거 요구는 수기라서 완화되지 않는다.** 5축 판정 각각이 `evidence` 를 갖춰야 하고,
   출처 없는 판단은 저장되지 않는다 (`CLAUDE.md` §3). 사람의 기억도 증거가 아니다.
   증거를 댈 수 없는 축은 `not_applicable` 로 닫고 그 사실을 적는다.
3. **판정과 근거는 결정 저널에 남는다.** 형식은 `09-operations.md` §2 의 진입 항목을
   그대로 쓴다 — 그 항목이 이미 thesis 전문·5축 판정·`cycle_confidence` 를 요구하므로
   새 형식을 만들 필요가 없다. 축 1 이 계산 불가한 테마는 `axis1_available = false` 를
   `key_uncertainties` 에 명시한다 (`04-value-trap.md` 축 1).
4. **이 수기 산출분도 `10-validation.md` §4 캘리브레이션 표본에 포함한다.** 제외하면
   M6~M7 구간의 판단이 통째로 검증 밖에 놓인다. 오히려 이 표본이 M7 이후와 비교할
   **기준선**이 된다 — 사람이 낸 `c` 와 `referee` 가 낸 `c` 중 어느 쪽이 잘 맞았는지는
   `10-validation.md` §6 의 "기계 권고를 뒤집은 결정" 과 같은 종류의 질문이다.
   저널 항목에 산출 주체(사람/`referee`)를 적어 두면 나중에 갈라서 볼 수 있다.

> 이 경로가 성립하는 이유는 위 절이 이미 말한 범위 축소에 있다. M6 의 운영 범위는
> 축 1 을 계산할 수 있는 테마 45개이고, 그 45개의 5축 판정은 사람이 월 단위로
> 감당할 수 있는 양이다. 축 1·2·4 는 L1 이 이미 숫자를 주므로 사람이 새로 만드는 것은
> 축 3·5 의 판단과 그 증거뿐이다.
> **80~90개 테마로 넓히는 것이 M7 의 역할이지, 수기 산출을 없애는 것이 M7 의 역할이 아니다** —
> M7 은 같은 규칙을 같은 제약 아래 더 넓게 적용하는 증폭기다.

