# 06 · 종목 선정 (L4)

테마가 확정되면 그 안에서 무엇을 살지를 정한다. **전 과정 결정론** — LLM 없음
(`CLAUDE.md` §4). 에이전트의 thesis 는 입력으로 쓰이되 랭킹은 코드가 한다.

> ## 2026-08-24 — 오늘의 선정 규칙은 이것 하나다
>
> **하드 제외(§2)를 통과한 적격 종목 전부 · 테마 내 동일가중.** 그 위에 아무 것도 없다.
>
> §5(바벨)·§6(종합 점수와 랭킹)이 적은 선정 규칙은 **선정 경로에서 빠졌다.** 두 절은 지우지
> 않고 그대로 두되, 각각 §5.1·§6.1 이 무엇이 왜 바뀌었는지를 그 아래에 쌓는다
> (`docs/14` §8 개정 이력 형식). §1 의 3축 논거와 §3 의 토크 축 서술도 **지우지 않는다** —
> 반증된 것은 이 표본에서의 예측력이지 서술 전체가 아니다 (§1 주석 · §3 주석).
>
> 근거는 `docs/backtest-l4.md`(판정)와 `docs/15` §4(선정 구조 비교), 경위와 사용자 결정은
> `journal/2026-08-24-l4-selection-retired.md`. 이 개정은 **결정의 기록**이지 새 설계가 아니다.

## 1. 왜 3축인가

원자재·사이클 테마의 종목 선정은 일반 팩터 투자와 다른 문제다.
사이클 저점의 이 산업에서는 **대부분의 기업이 적자**고, 밸류 팩터는 분모가 음수라 무의미하고,
퀄리티 팩터는 정확히 반대 방향을 가리킨다 (**우량주가 가장 덜 오른다**).

실제로 작동하는 축은 셋이다:

| 축 | 묻는 것 | 실패 시 결과 |
|---|---|---|
| **생존 S** | 논지가 맞을 때까지 살아남는가 | 논지가 맞아도 0 원. 사이클 저점 투자의 1차 사망 원인 |
| **토크 T** | 맞으면 얼마나 크게 움직이는가 | 맞아도 시장 수익. 이 전략을 할 이유가 없어짐 |
| **타이밍 M** | 지금 들어가도 되는 상태인가 | 6개월 더 하락 후 물타기 여력 소진 |

**S 와 T 는 구조적으로 상충한다.** 저비용 대형 생산자는 안 죽지만 3배 안 간다.
고비용 레버리지 생산자는 5배 가지만 그 전에 파산할 수 있다.
그래서 하나로 합산하지 않고 **바벨로 구성한다** (§5).

> **2026-08-24 표시 — 위 논거는 그대로 둔다. 다만 아래 사실을 함께 읽는다.**
>
> `docs/backtest-l4.md` 의 Q2 는 세 축의 **단독** 12개월 rank-IC 를 이렇게 쟀다:
>
> | 축 | 테마 내 12M rank-IC [95% CI] | 읽는 법 |
> |---|---|---|
> | S̃ (생존) | **+0.0449** [+0.0203, +0.0658] | 이 경로에서 일한다 |
> | T̃ (토크) | **−0.0425** [−0.0699, −0.0162] | **반대로 일한다** — 부호가 서술과 어긋난다 |
> | M̃ (타이밍) | **+0.0327** [+0.0096, +0.0539] | 이 경로에서 일한다 |
>
> 반증된 것은 **이 표본·이 창(12개월)·이 정의에서 T̃ 의 부호**이지 §1 의 서술 전체가 아니다.
> 표본이 사실상 하나이고(`docs/10` §2.4), 이 검정이 못 재는 것이 `docs/14` §5 · `docs/15` §6 에
> 열거돼 있다 — 특히 **비중이 없고, 물타기 여력이 원리적으로 보이지 않으며, L3 확신도가
> 빠져 있고, 테마 선정이 조건으로 들어가지 않는다.**
>
> **축 가중치·축 정의는 하나도 옮기지 않았다** (`docs/14` §4.2 둘째 행 · `docs/15` §5.1).
> 검정이 "이 축은 일하지 않는다" 고 말하면 그 사실을 기록하고 값은 그대로 둔다
> (`CLAUDE.md` §1). **T̃ 축 자체를 존치할지는 이 개정이 정하지 않는다** — 별개 문제이고,
> 정하려면 새 사전 등록이 필요하다.

## 2. 생존 축 (S)

`fin-checkup` 의 `metrics/redflags.py` 를 이식한다.

| 지표 | 계산 | 임계 |
|---|---|---|
| `cash_runway_q` | 현금 / max(0, −분기 FCF) | **< 4분기 = 하드 제외.** 논지 horizon 안에 죽는다 |
| `net_debt_ebitda` | 순부채 / TTM EBITDA (EBITDA≤0 이면 순부채/시총으로 대체) | > 4× 감점, > 6× 하드 제외 |
| `maturity_wall_24m` | 24개월 내 만기 부채 / 시총 | > 0.5 하드 제외 (`04-value-trap.md` 축 5) |
| `interest_coverage` | EBIT / 이자비용 | < 1 감점 |
| `dilution_3y` | 주식수 3년 CAGR | > 15%/y 감점. **테마 레벨에선 저점 신호, 종목 레벨에선 주주가치 파괴** |
| `going_concern` | 감사의견 계속기업 의문 | 하드 제외 (에이전트 또는 SEC 파싱) |
| `liquidity` | 20일 평균 달러 거래대금 | < $2M 감점 (자금 규모 작아 완화 가능, 값은 설정) |
| `price_floor` | 종가 | < $2 감점 (상장폐지·역분할 리스크) |

> **하드 제외는 스코어가 아니다.** 아무리 토크가 커도 4분기 안에 현금이 마르는 기업은
> 후보에서 사라진다. AG·SBSW·MP·ALM 형 트레이드에서 이 필터 하나가 최대 손실을 좌우한다.

## 3. 토크 축 (T)

논지가 맞을 때 이익이 얼마나 폭발하는가. 사이클 투자에서 **초과수익의 원천 전부**다.

| 지표 | 계산 | 읽는 법 |
|---|---|---|
| `margin_headroom` | (섹터 자기이력 P75 마진 − 현재 마진) | 마진 회복 여지 |
| `opleverage` | ΔEBITDA% / Δ매출% (12분기 회귀 기울기) | 고정비 비중의 실측 대용 |
| `fixed_cost_ratio` | (매출 − 변동비 추정) / 매출 | 회계 분해 근사 |
| `price_beta_hist` | 직전 상승 국면에서 EBITDA 의 상품가 탄력성 | **가장 직접적. 다만 표본 1~2 사이클** |
| `equity_leverage` | (순부채 + 시총) / 시총 = EV/시총 | 부채가 주식 수익을 증폭. **S 축과 정면 상충** |
| `marginal_producer` | 마진이 섹터 하위 25% | 원가곡선 상단 = 최대 토크 (`04` 축 4) |

**충돌 처리**: `equity_leverage` 와 `marginal_producer` 는 S 축의 감점 항목과 같은 사실을 본다.
합산하지 않고 **두 축의 값을 둘 다 보존해 바벨 배치에 쓴다.**

> **2026-08-24 표시 — 위 서술과 지표 정의는 그대로다. 두 가지를 덧붙인다.**
>
> 1. **"초과수익의 원천 전부" 라는 문장은 이 경로에서 확인되지 않았다.** T̃ 단독 12M rank-IC 가
>    **−0.0425** [−0.0699, −0.0162] 로, CI 가 0 을 넘지 않고 **부호가 반대**다
>    (`docs/backtest-l4.md` Q2). §1 의 표와 그 아래 단서를 함께 읽는다.
> 2. **마지막 문장의 "바벨 배치에 쓴다" 는 오늘 성립하지 않는다.** 바벨은 선정하지 않는다
>    (§5.1). `t_pct`·`tp_*` 는 `ranking.csv` 에 관찰 지표로 계속 실린다.
>
> **지표 정의도 임계도 하나 옮기지 않았다.**

## 4. 타이밍 축 (M)

`momentum` 저장소 이식. 개별 종목 레벨은 원본 그대로 쓸 수 있다.

| 지표 | 정의 |
|---|---|
| `stage2` | Minervini: `close > SMA150 > SMA200`, `SMA200` 상승 1개월+, 52주 저점 대비 +30%, 고점 대비 −25% 이내 |
| `rs_rating` | IBD 방식 백분위 (vs 전체 유니버스) |
| `vcp_base` | VCP 수축 패턴 존재 + 거래량 dry-up |
| `from_52w_low` | 저점 대비 상승률 |
| `above_50d` | 50일선 위 여부 |
| `rvol_expansion` | 최근 거래량 확장 |

> **타이밍 축은 가중치가 낮다** (§6). 이유: 물타기가 가능하고 MDD 30% 를 감내하므로
> 진입 정밀도의 가치가 낮다. Stage 2 를 **요구하면** 테마 진입이 항상 늦어진다 —
> 우리는 잊혀진 테마에 **먼저** 들어가려는 것이지 확인된 추세를 따라가려는 게 아니다.
> M 은 순위 조정과 **사다리 1단의 시점 힌트**로만 쓴다.

## 5. 바벨 구성

테마당 2~4 종목, 두 그룹으로:

| 그룹 | 선정 기준 | 역할 | 테마 내 비중 |
|---|---|---|---|
| **앵커 (survivor)** | S 상위 · 저비용 생산자 · 대형 | 논지가 늦게 맞아도 안 죽는다. 물타기 여력의 근거 | 55~70% |
| **토크 (torque)** | T 상위 · S 하드필터 통과 | 논지가 맞을 때의 수익 대부분 | 30~45% |

옵션 그룹 — 테마에 존재할 때만:
- **로열티/스트리밍** (금은: FNV·WPM·RGLD 형) — 원가 리스크 없이 가격 노출. 앵커 대체 가능
- **미드스트림/인프라** (에너지) — 물량 노출, 가격 노출 낮음. 앵커 성격
- **ETF** — 구성종목 수가 `min_constituents` 미만이거나 개별 리스크가 과도할 때 대체

> **2026년 원자재 트레이드의 사후 진단**: AG·SBSW·MP·ALM 은 **4개 전부 토크 그룹**이었다.
> 결과는 좋았지만, 앵커 0개는 설계가 아니라 우연이었을 가능성이 높다.
> 리포트는 매 구성마다 **앵커/토크 비율을 명시**해 이 선택이 의도적이도록 만든다.
> (앵커 0% 를 금지하지는 않는다 — 확신도가 높으면 합리적 선택이다. 다만 **보이게** 한다.)

### 5.1 개정 — 2026-08-24 · 바벨은 더 이상 선정하지 않는다

**위 §5 를 지우지 않는다.** 아래가 오늘 도는 규칙이고, 위는 그 자리에 무엇이 있었는지다.

| 무엇 | 바뀐 것 | 왜 |
|---|---|---|
| 테마 내 선정 | 바벨 2~4 종목 → **하드 제외 통과 종목 전부 · 동일가중** | `docs/15` §4 — B0(현행 바벨)·B1(시총 상위 3)·B2(토크 상위 3) 중 **아무도 B3(하드 제외 통과 전부 동일가중)를 이기지 못했다.** `docs/15` §5 셋째 행이 그 결과에 대한 조치를 **결과를 보기 전에** 정해 두었다: "L4 의 선정 규칙을 버린다" |
| 앵커/토크 라벨 | 선정 라벨 → **관찰 지표** (`ranking.csv` 의 `barbell_obs` 열) | 라벨 자체를 지우면 "옛 규칙이라면 무엇을 골랐을까" 를 더 이상 못 본다. 코드는 `barbell.classify` 를 그대로 계산하되 선정 경로에서 뺐다 |
| `ANCHOR_S_MIN`(0.5) · `TORQUE_S_EXCLUDE_LE`(0.25) · `n_anchor` 배분식 · `top`(4) | **하나도 안 바뀜** | `docs/15` §5.1 이 금지한 정확히 그 값들이다. **버리는 것과 옮기는 것은 다르다** |
| 하드 제외 §2 | **하나도 안 바뀜** | 아래 참조 |

**동일가중은 가중치를 정한 것이 아니라 정하지 않은 것이다.** 적격 종목 사이를 가를 근거가 이
경로에서 확인되지 않았으므로 가르지 않는다. 새 가중치를 도입한 것이 아니다 (`CLAUDE.md` §1).
비중 자체는 여전히 L5 가 정한다 (`docs/07`) — L4 는 비중을 내지 않는다.

**하드 제외(§2)는 그대로 남는다.** `docs/backtest-l4.md` 의 Q3 은 E1(런웨이<4분기)·
E2(ND/EBITDA>6×)·E3(만기벽>0.5)의 **초과수익 차 CI 가 셋 다 0 을 포함**한다고 판정했다 —
알파가 아니다. 그러나 **사망률 차의 CI 하한이 셋 다 0 을 넘었다** — 제외군이 실제로 더 많이
죽었다. 메커니즘은 확인됐고 수익 기여는 확인되지 않았다. `docs/14` §4.2 넷째 행이 이 경우의
조치를 미리 정해 두었다: **임계를 옮기지 않는다.** 하드 제외의 근거는 수익률이 아니라 §2 의
도메인 서술이다.

**§5 의 옵션 그룹(로열티·미드스트림·ETF)** 은 L4 가 애초에 태깅하지 않았다. 바뀐 것 없다.

**무엇을 이 개정이 재지 못했는가**: `docs/15` §6 이 여덟 가지를 열거한다. 특히 **비중이 없고**
(이 검정은 전부 동일가중이었다), **앵커의 "물타기 여력" 이 원리적으로 보이지 않으며**
(경로 의존이고 L5 사다리에 달려 있다), **L3 확신도와 테마 선정이 조건으로 들어가지 않는다.**
B0 가 졌다는 것이 바벨 설계의 반증은 아니다 — 이 창에서 그것으로 종목을 고를 근거가 서지
않았다는 것이다.

## 6. 종합 점수와 랭킹

```
rank_score = 0.40·S̃ + 0.40·T̃ + 0.20·M̃          (틸데 = 테마 내 횡단면 백분위)
```

- 하드 제외 필터(§2)를 먼저 적용한 뒤 랭킹
- 앵커 선정: `S̃` 상위에서 `T̃` 가 가장 높은 것
- 토크 선정: `T̃` 상위에서 `S̃` 하위 25% 를 뺀 것
- 가중치 0.40/0.40/0.20 은 **선언**이다 (`CLAUDE.md` §1)

### 6.1 개정 — 2026-08-24 · `rank_score` 는 관찰 지표다

**위 §6 을 지우지 않는다.** 아래가 오늘의 지위다.

`rank_score`(코드에서는 `composite`) · `rank` · `group` · 3축 백분위(`s_pct`·`t_pct`·`m_pct`) ·
바벨 라벨은 **계속 계산되어 `ranking.csv` 에 실린다.** 열을 없애지 않는다 — 하위 호환
(`pipeline.assemble._RANKING_REQUIRED` · `l5.inputs` 의 `role`) 때문이고, 관찰을 계속하기
위해서다. 바뀐 것은 **지위**다: **이 값들 중 무엇도 어떤 종목을 사는지를 정하지 않는다.**

| 왜 | 근거 |
|---|---|
| 종합 점수의 예측력이 관문을 통과하지 못했다 | `docs/backtest-l4.md` Q1 **FAIL** — `rank_score` 주 창 12개월 테마 내 rank-IC **+0.0144** [−0.0008, +0.0266]. CI 가 0 을 포함한다 |
| `docs/14` §4.2 둘째 행이 이 결과의 조치를 미리 정해 뒀다 | "`ranking.csv` 는 계속 산출하되 `composite` 순위를 **관찰 지표로 강등**한다" |
| 애초에 선정에 배선돼 있지도 않았다 | `journal/2026-08-23-l4-rank-score-unwired.md` — 가중치 0.40/0.40/0.20 은 **아무 것도 고르지 않았다.** 어떤 값으로 바꿔도 `msa picks` 가 고르는 종목은 같았다. 선정은 `barbell.classify` 가 `t_pct`·`s_pct`·`marginal_producer` 셋만 보고 했다. 이 개정은 그 사실을 문서와 산출물 문구에 반영하는 일이기도 하다 |
| M 축도 같다 | 같은 저널. `m_pct` 는 선정에 관여한 적이 없다. §4 가 "순위 조정" 이라 부른 그 순위가 선정과 무관한 표시용 등수였다. `m_pct` 는 관찰 지표로 남는다 — **§4 의 서술은 지우지 않는다** |

**`AXIS_WEIGHTS`(0.40/0.40/0.20) · `S_WEIGHTS` · `T_MIN_INPUTS` · `RUNWAY_CAP_Q` 는 하나도
옮기지 않았다** (`docs/14` §4.3). 축 정의도 그대로다.

산출물에서 이 사실이 보이는 곳: `ranking.csv` 의 `group`(전 행 `ELIGIBLE`)·`barbell_obs` 열 ·
`report.txt` 의 "선정" 문단과 종목 블록 머리의 `[관찰 #N · 종합 …]` · `meta.json` 의
`selection`/`barbell_observation` 블록 · `picks.csv` 의 `notes` · 일간 다이제스트 머리말.

### 6.2 열린 질문 — 운용 가능한 K 는 정해져 있지 않다

**이 개정이 정하지 않은 것을 명시한다.**

채택된 구조(B3)는 적격 종목을 **전부** 보유한다. 테마당 수십 종목이 될 수 있고, 그것은 실제
운용과 맞지 않는다. `docs/15` §4 는 B1(시총 상위 3)도 B3 와 구분되지 않았다고 보고했으므로
소수 종목으로 줄일 여지가 있어 **보이지만**, **결과를 보고 B1 을 고르는 것은 사후 선택이다**
(`docs/15` §5.1 — "후보를 사후에 추가하지 않는다").

따라서:

- **지금 K 를 정하지 않는다.** 코드는 선언된 조치(적격 전부·동일가중)를 그대로 집행한다.
- **"운용 가능한 K 를 어떻게 정할 것인가" 는 새 사전 등록이 필요하다** — 새 번호의 문서 ·
  후보를 데이터를 더 보기 전에 고정 · `docs/14` §4.1 과 **같은** 합격 기준 · 시도 수 재정산
  (`docs/14` §4.3 이 정한 유일한 경로). 이 절은 그 문을 열지 않는다.
- 그때까지 종목 수를 줄이는 것은 **사람이 산출물을 보고 하는 일**이다. `msa portfolio-inputs
  --top N` 과 L5 의 상한이 그 자리이며, **그 N 은 L4 의 규칙이 아니다.** 자를 때 쓰는 `rank`
  가 관찰 지표라는 사실(§6.1)을 그대로 안고 자르는 것이다.
- 함께 열린 것 둘: **T̃ 축의 존폐**(§1 표 — 부호가 반대였다)와 **동일가중과 L5 사이징의 관계**
  (검정은 전부 동일가중이었으나 실제 비중은 L5 의 SOCP 가 정한다 — `docs/15` §6 #3).
  어느 쪽도 이 개정이 정하지 않는다.

## 7. 리포트에 반드시 표기되는 것

각 후보 종목에 대해:

```
MP · rare_earth · TORQUE
  S 0.42  runway 11q · ND/EBITDA n/a(적자) · 만기벽 0.08 · 희석 3y +9%/y
  T 0.88  마진여지 P75까지 +22pp · opleverage 3.4× · 한계생산자 아니오
  M 0.61  Stage2 아니오 · RS 74 · VCP 베이스 있음 · 52wL +18%
  하드필터: 통과
  주의: 단일 광산 의존 (Mountain Pass), 중국 정책 리스크 (04 축5 warning)
```

**`S` 의 하위 항목을 접지 않는다.** 종합 점수만 보면 4분기 런웨이와 12분기 런웨이가
같은 0.42 로 보인다. 사이클 저점 투자에서 그 차이가 전부다.

> **2026-08-24 표시** — 위 예시의 머리 `MP · rare_earth · TORQUE` 는 오늘
> `MP · rare_earth · ELIGIBLE   [관찰 #2 · 종합 0.80 · 바벨 ANCHOR]` 로 나온다. 표기하는 항목은
> 그대로이고(§7 의 요구는 살아 있다), **그 값들이 선정하지 않는다는 사실이 문구에 붙었다**
> (§6.1). 리포트 머리에는 "선정: 적격 N 종목 전부 · 테마 내 동일가중" 이 먼저 나온다.

## 8. 구현 노트 (M5) — 문서가 비워 둔 곳을 어떻게 채웠는가

`src/msa/l4/` (`features`·`axes`·`barbell`·`picks`), 명령 `msa picks <theme> [--asof --top --no-write
--no-physical]` (`--top` 은 2026-08-24 부터 **관찰용 바벨 라벨 수**다 — §5.1), 산출물 `state/picks/<date>/<theme>/{ranking,excluded}.csv · report.txt · meta.json`.
아래 값은 전부 **선언**이며 데이터에 맞춰 고르지 않았다 (`CLAUDE.md` §1). 바꾸려면 근거를 여기와
커밋 메시지에 남긴다. 사후 대조 결과는 `docs/picks-m5-check.md`.

### 8.1 입력이 없어 적용하지 못한 것 — 리포트·`meta.json` 에 매번 적힌다

| 문서의 입력 | 실측 | 처리 |
|---|---|---|
| `going_concern` | 감사의견은 스토어에 없다 (에이전트/SEC 파싱 영역) | 하드 제외 **미적용**. `inputs_unavailable` 에 표기 |
| `maturity_wall_24m` | SF1 에 만기 스케줄이 없다. 유동부채 `debtc`(12개월 내)만 있다 | `maturity_wall_12m = debtc / 시총` 으로 대용. 12m ⊂ 24m 이라 0.5 초과 제외는 오제외가 없고, 13~24개월 벽은 놓친다 |
| `price_beta_hist` 의 상품가 | 테마 `physical_ref` 가 있고 데이터가 있을 때만 (ETF 는 벌크 zip, manual 은 `state/physical/manual/`) | 없으면 NaN + 사유. `rare_earth` 는 `physical_ref` 미선언, `lithium` 은 manual 파일 없음 |
| 레드플래그 `partial_capital_impairment` | SF1 에 자본금(`capital_stock`) 없음 | 계산 불가로 표기 (`vendor/redflags.NOT_COMPUTABLE`) |
| 20-F 해외발행사 재무 | SBSW·SQM·BVN 은 `fundamentals` 에 **행이 0개** | `fund_status=none` → 생존 필터 판정 불가로 제외, 사유 구분 |
| `short_interest` · 컨센서스 · `insiders`/`institutions` | 각각 100% null · 0행 · 있음 | 이 문서가 요구하지 않아 **미사용**. `meta.json` `inputs_unused` 에 기록만 |

### 8.2 계산식 — 문서가 이름만 준 곳

| 항목 | 구현 | 근거 |
|---|---|---|
| PIT | 전부 `datekey ≤ asof` · 같은 `calendardate` 는 최초 보고분 (L1 과 동일). 스냅샷 지표도 예외 없음 | 한 모듈에 두 규칙을 섞지 않는다. 백테스트·오늘의 스캔이 같은 함수를 `asof` 만 바꿔 부른다 |
| TTM | 직전 4분기, 4개 전부 있고 4번째가 **300일 이내** | L1 은 400일 — 4개 분기말 정상 간격 273일, 한 분기 빠지면 365일이라 400일은 한 분기 결측을 통과시킨다. 테마 합산에선 희석되지만 종목 런웨이에선 25% 오차 |
| `cash_runway_q` | 현금 / (TTM FCF 의 분기 평균 소진). FCF ≥ 0 → ∞. `fcf` null 이면 `ncfo + capex`. 현금흐름표 자체가 없으면 NaN → **판정 불가로 하드 제외** | 광업 capex 는 분기 덩어리라 단일 분기 FCF 는 4분기 제외를 한 분기 착시로 발동시킨다. 판정 불가를 통과로 두면 생존 축 1차 항목이 빈 종목이 상위에 온다 (실측: 반기 보고 해외발행사 GMTL·IPX·CRML 이 그렇게 1위에 올랐었다) |
| `net_debt_ebitda` | (부채 − 현금)/EBITDA_ttm, EBITDA ≤ 0 이면 /시총 (`nd_basis` 에 표기) | 문서 그대로 |
| `dilution_3y` | 최신 `sharesbas` 대비 36개월(±60일) 전 행의 CAGR — 날짜로 찾는다 | 행 위치(lag 12)는 결측 분기가 있으면 기간이 틀어진다 |
| `opleverage` | 12분기 QoQ ΔEBITDA_ttm ~ Δ매출_ttm 회귀 기울기(`incremental_margin`) ÷ max(\|현재 마진\|, 0.05). 최소 8쌍. 차분이 상수(완전 선형)면 NaN | "ΔEBITDA%/Δ매출%" 는 EBITDA≈0 에서 발산. 분모 5pp 바닥은 순위가 분모 잡음에 지배되지 않게 하는 값 |
| `fixed_cost_ratio` | (매출_ttm − `cor`_ttm)/매출_ttm | 문서의 "(매출 − 변동비 추정)/매출" 에서 변동비 = 매출원가 |
| `margin_headroom` | 테마 Σebitda_ttm/Σrevenue_ttm 분기 시계열(최근 40분기, 분기당 ≥ 2 구성원)의 P75 − 현재 마진. 시계열 < 12분기면 NaN | "섹터 자기이력 P75" 의 섹터 = 테마 |
| `marginal_producer` | 현재 마진이 상장 구성원(매출 > 0) 하위 25%. 그런 구성원이 4개 미만이면 NA | 사분위는 관측 4개 미만에서 정의되지 않는다 (실측: `silver_miners` 는 2개) |
| `price_beta_hist` | 참조가격 10년 저점 → 그 후 고점(≥ 12개월) 구간의 (EBITDA_ttm 변화 / 저점 시 매출_ttm) ÷ log(P₁/P₀) | "EBITDA %변화" 는 적자에서 정의되지 않아 매출로 정규화(마진 pp / 가격 100%). 표본 1 사이클 — 문서의 경고 그대로 |
| `equity_leverage` | (순부채 + 시총)/시총 | 문서 그대로 |
| `rs_rating` | 0.4·r3m + 0.2·r6m + 0.2·r9m + 0.2·r12m 의 **전체 유니버스**(그날 가격 있는 전 종목, 5개 거래일 슬라이스만 읽음) 백분위 1~99 | IBD 공표 방식 |
| `stage2` | `close > SMA150 > SMA200`, SMA200 이 21거래일 전보다 위, 52주 저점 +30% 이상, 고점 −25% 이내 | 문서 그대로 (Minervini) |
| `vcp_base` | 252일 창 · 피벗 좌우 5일 · 마지막 수축 ≤ 4개 중 2개 이상이 단조 감소 · 10일 거래량 < 50일 | L1 `vcp_index_score` 와 같은 피벗 파라미터 + dry-up |
| `rvol_expansion` | 20일 평균 거래량 / 50일 평균 거래량 | 1 초과 = 확장 |
| 상장 판정 | asof 이전 10거래일(SPY 달력) 안에 가격 행 | 폐지·거래정지 제외 수를 보고 |
| 최신 분기 신선도 | `calendardate ≥ asof − 15개월` (L1 과 동일) | 그보다 오래되면 `fund_status=stale` → 제외 |

### 8.3 축 점수 — 문서가 가중치를 비워 둔 곳

| 항목 | 선언 | 근거 |
|---|---|---|
| S 원점수 | `0.40·runway_score + 0.30·leverage_score + 0.30·penalty_score` | 런웨이는 §2 가 "1차 사망 원인" 이라 부른 항목 → 최대. 레버리지는 두 번째 하드 항목. 감점 5종 + 레드플래그 4종은 등가의 보조 신호 |
| `runway_score` | `clip(runway_q / 8, 0, 1)`, FCF ≥ 0 → 1 | 논지 horizon 6~18개월(`05` `horizon_months`) 상한 + 자금조달 6개월 = 8분기. 그 너머는 논지 창 안의 생존을 더 늘리지 않는다 |
| `leverage_score` | `1 − clip(ND/EBITDA / 6, 0, 1)`, 순현금 → 1 | 하드 제외 임계 6× 를 0 점으로 두는 선형 감점 |
| `penalty_score` | `1 − 발동 수 / 평가 가능 수` (ND/EBITDA > 4 · 이자보상 < 1 · 희석 > 15%/y · ADV < $2M · 종가 < $2 · 레드플래그 4종) | 감점 항목끼리 우열 근거가 없어 등가. 입력 없는 항목은 분모에서 뺀다 — 없는 것을 통과로 세지 않는다 |
| T 원점수 | 6개 구성 요소(`margin_headroom`·`opleverage`·`fixed_cost_ratio`·`price_beta_hist`·`equity_leverage`·`marginal_producer`)의 테마 내 백분위 평균, 불리언은 0/1, 전부 높을수록 ↑ | 문서가 순서를 주지 않았다. `price_beta_hist` 는 "가장 직접적" 이지만 "표본 1~2 사이클" 이라 가중하지 않는다 |
| M 원점수 | `stage2`·`rs_rating/100`·`vcp_base`·`above_50d`·pct(`from_52w_low`)·pct(`rvol_expansion`) 의 평균 | §4 의 6개 등가. M 은 종합 가중 0.20 으로 이미 낮다 |
| 축 최소 입력 | T·M 모두 6개 중 **3개 이상**, 아니면 NaN | 소수 구성 요소의 평균은 그 축이 아니라 다른 것을 잰다 (실측: `above_50d` 하나로 M=1.0 이 나왔었다) |
| 종합 | `0.40·S̃ + 0.40·T̃ + 0.20·M̃` (§6). 축이 NaN 이면 가용 축 가중치를 재정규화하고 `composite_partial` 표시 | 빈 축을 0 으로 두면 순위가 결측에 지배되고, 표시 없이 재정규화하면 조용한 절단. **2026-08-24: 식은 그대로이고 지위만 관찰로 내려갔다 (§6.1)** |
| 순위 동률 | 종합 ↓ → S̃ ↓ → 티커 ↑ | 결정론 |
| 바벨 "S̃ 상위" | `S̃ ≥ 0.5` 이고 `marginal_producer` 가 True 가 아닌 것 중 T̃ 최고 | 중앙값은 표본 크기에 무관. "저비용 생산자" — 한계생산자는 정의상 아니다. **2026-08-24: 값은 그대로이고 지위만 관찰로 내려갔다 (§5.1) — `ranking.csv` 의 `barbell_obs`** |
| 바벨 수 배분 | `n_anchor = max(1, top // 2)`, 나머지 토크(`S̃ ≤ 0.25` 제외, **T̃ 가 계산된 종목만**). 후보가 모자라면 자리는 비고 비율에 드러난다 | §5 비중 55~70/30~45 를 수로 옮기면 절반 전후. T̃ NaN 은 "T̃ 상위" 일 수 없다. 비중 자체는 L5. **2026-08-24: `--top` 은 이제 관찰용 라벨 수일 뿐 선정 개수가 아니다 (§5.1)** |

### 8.4 아직 없는 것
- 로열티/스트리밍·미드스트림 옵션 그룹 태깅 (§5) — Sharadar 에 라벨이 없다. 실측에서 MTA·ELE·VOXR·TFPM 같은 로열티사가 일반 생산자와 섞여 랭킹된다
- 에이전트 thesis 입력 (§7 의 `주의:` 줄 재료) — M7 뒤
- ~~L4 축별 rank-IC 백테스트~~ — 2026-08 에 실행됐다. 판정은 `docs/backtest-l4.md`, 사전 등록은 `docs/14`·`docs/15`, 조치는 §5.1·§6.1

### 8.5 L5 로 넘어가는 것 — `msa portfolio-inputs` (배선 W1, 2026-08-23)

`ranking.csv` 는 L5 가 직접 읽지 않는다. `src/msa/pipeline/assemble.py` 가 `state/picks/<date≤asof>/<theme>/ranking.csv`
에서 **`group`(선정 라벨) · `barbell_obs`(관찰) · `rank` · `composite` · `price` · `adv20_usd` · S̃/T̃/M̃ · `penalties` · `red_flags` ·
`composite_partial` · 축 입력 결측** 만 꺼내 L5 의 `picks.csv` (`docs/07` 구현 노트 "배선 W1") 로 옮긴다
(`picks.RANKING_EXPORT_COLUMNS` · `picks.read_ranking` · `features.ENTRY_PRICE_FEATURE/LIQUIDITY_FEATURE`).
- `role` 은 `group` 에서만 온다 — `ANCHOR`→`anchor`, `TORQUE`→`torque`. 라벨이 빈 행(하드 필터는 통과했으나
  바벨에 들지 않은 종목)은 L5 로 가지 않고 **수와 사유**로 보고된다. §5 옵션 그룹(로열티·미드스트림·ETF)은
  §8.4 그대로 태깅이 없어 매핑도 없다.

  > **2026-08-24 개정** — `group` 은 이제 적격 종목 **전 행이 `ELIGIBLE`** 이고 `role` 도 전 행이
  > `eligible` 이다. 열은 그대로 있고(하위 호환), **행을 가르지 않는다**: 하드 제외를 통과한 종목은
  > 전부 L5 로 간다 (§5.1). 옛 스냅샷의 `ANCHOR`/`TORQUE` 도 계속 읽힌다. `barbell_obs` 열이
  > **추가**되어 옛 규칙의 라벨을 관찰로 싣는다. 따라 나오는 것 둘:
  > (a) L5 의 `anchor_share` 진단이 0 이 된다 — L4 가 앵커를 **지정하지 않기** 때문이지 앵커 비중을
  > 0 으로 정한 것이 아니다. (b) `positions.yaml` 스키마가 `anchor|torque` 만 받으므로 `eligible` 은
  > `torque` 로 사상되고 그 사실이 제안서 note 에 찍힌다 — 이것은 **스키마 사정이지 분류가 아니다.**
  > (c) `--top N` 은 사람이 주는 상한이고 L4 의 K 가 아니다 (§6.2).
- L4 가 내지 않는 것은 L5 쪽 열을 비운다 (`idio_vol_ann`·밸류 P50/P75 회복가·직전 사이클 고점가·`split_first_leg`)
  — 채우려면 **이 계층**이 특성을 추가해야 한다 (§8.4 에 추가: 종목 고유 변동성 · 테마 밸류 백분위 가격).

## 9. 개정 이력

`docs/14` §8 과 같은 규약 — **개정을 지우지 않고 쌓는다** (`CLAUDE.md` §6 저널 규약과 같은 태도).

### 2026-08-24 개정 — 무엇을, 왜

**결정한 사람**: 사용자. 사전 등록된 두 검정(`docs/14`·`docs/15`)이 끝난 뒤, `docs/15` §5 가
**결과를 보기 전에** 적어 둔 조치표의 셋째 행("아무도 B3 를 못 이김")을 집행한 것이다. 코드가
결과를 보고 스스로 고른 것이 아니다.

| 절 | 무엇 | 왜 |
|---|---|---|
| 머리말 (신설) | 오늘의 선정 규칙 한 줄 — 하드 제외 통과 전부 · 테마 내 동일가중 | 이 문서를 처음 읽는 사람이 §5·§6 을 현행 명세로 읽지 않게 한다 |
| §5.1 (신설) | 바벨은 관찰 지표로 강등. §5 원문은 보존 | `docs/15` §4 — B0·B1·B2 중 아무도 B3 를 이기지 못했다 (B0−B3 +0.76%p [−0.29, +2.76] · B1−B3 +0.92%p [−2.36, +4.48] · B2−B3 +0.86%p [−2.25, +5.30]) |
| §6.1 (신설) | `rank_score`·`rank`·`group`·3축 백분위는 관찰 지표. §6 원문은 보존 | `docs/backtest-l4.md` Q1 FAIL (+0.0144 [−0.0008, +0.0266]) + `docs/14` §4.2 둘째 행의 미리 정한 조치 + 애초에 배선돼 있지 않았다는 사실(저널 2026-08-23) |
| §6.2 (신설) | 운용 가능한 K 를 **정하지 않는다**고 명시 | B1 을 결과를 보고 고르는 것은 사후 선택이다 (`docs/15` §5.1). 새 사전 등록이 필요하다 |
| §1 · §3 (주석 추가) | T̃ 단독 IC 가 이 경로에서 **음수**였다는 사실과 출처. 논거는 **삭제하지 않음** | 반증된 것은 이 표본에서의 예측력이지 서술 전체가 아니다. `docs/15` §5 셋째 행이 "서술을 지우지 않고 확인되지 않았다고 적는다" 고 요구한다 |
| §7 (주석 추가) | 리포트 머리 형식 변화 표기 | 표기 요구(§7)는 그대로 살아 있다 |

**이 개정이 바꾸지 않은 것** — 명시한다:

- **하드 제외 §2 의 임계 전부**: 런웨이 4분기 · ND/EBITDA 6× · 만기벽 0.5 · 감점 임계 5종
- **축 정의 §2·§3·§4 전부**, 그리고 3축 계산 자체 (`axes.survival`·`torque`·`timing`)
- `AXIS_WEIGHTS`(0.40/0.40/0.20) · `S_WEIGHTS` · `T_MIN_INPUTS`·`M_MIN_INPUTS` · `RUNWAY_CAP_Q`
- `ANCHOR_S_MIN`(0.5) · `TORQUE_S_EXCLUDE_LE`(0.25) · `n_anchor` 배분식 · `DEFAULT_TOP`(4)
- `ranking.csv` 의 어느 열도 삭제되지 않았다 (`barbell_obs` 가 **추가**됐다)

**새로 만든 가중치·임계·K 는 없다.** 동일가중은 "가중치를 정한 것" 이 아니라 "정하지 않은 것"
이다 (`CLAUDE.md` §1).

**정본**: 판정은 `docs/backtest-l4.md`·`docs/15` §4, 경위와 결정은
`journal/2026-08-24-l4-selection-retired.md`. `docs/14`·`docs/15`·`docs/backtest-l4.md` 는 이
개정에서 **한 글자도 고치지 않았다** — 사전 등록과 판정은 확정본이다.
